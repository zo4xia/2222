# 校准后的需求与重构路线 (Calibrated PRD & Priority)

## 1. 核心业务价值与需求校准 (Core Mission)
- **业务载体**：小学数学解题引导视频与交互式手绘板书生成系统。
- **教学底线（清华附小教学法）**：
  1. 必须包含完整的四环一体（题目锚定、启发分析、解题推导、总结升华）。
  2. 必须包含循序渐进的引导用语、问为什么、回顾学过知识、建立解题世界观。
  3. 严禁压缩、删减或机械化口播内容；禁止将教学引导压扁为死板的步骤答案。
- **工程目标（标准工业化与解耦）**：
  1. **表现层纯净化**：UI 组件只管视图交互，严禁在组件内直接调用 `fetch`。
  2. **服务层统一收口**：所有 API 请求统一走 `services/*`，统一响应封套与错误捕获。
  3. **配置中枢统一**：消除 3 份重复的配置存储代码，实现统一响应式管理。
  4. **零报错启动与自检**：修复全部测试断言偏差，确保 `npm run check:*` 100% 绿灯。

---

## 2. 重构执行路线与分期规划

| 阶段 | 核心任务 | 关键交付物 | 验收标准 |
| :--- | :--- | :--- | :--- |
| **阶段三：骨架施工** | 1. 页面与组件四分类<br/>2. 建立 `services/` 统一接入层<br/>3. 提取通用配置工厂 `createApiConfigStore` | `module_dependency_graph.md`<br/>`decoupling_strategy.md` | 耦合点显式标记，单向依赖链确立 |
| **阶段四：管线铺设** | 1. 接入 TTS、Deliverable、Cleanup 统一服务<br/>2. 修复 `server/proxySelfCheck.js` 断言偏差<br/>3. 校验数据流 组件->Service->API | `dataflow_diagram.md`<br/>`api_spec.yaml` | 全局搜索零处 UI 直连 fetch，自检全部通过 |
| **阶段五：精装验收** | 1. 全链路冒烟验证（识别->生成->质检->交付物打包）<br/>2. P0-P4 评级归零<br/>3. 固化规范与打包最终交付物 | `test_report.md`<br/>`code_standards.md` | `npm run check:*` 绿灯，零报错启动 |
