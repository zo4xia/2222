# 04 · 需求校准表（文档 ↔ 代码 逆向推导）

> 阶段一·深度扫描产出 ｜ 生成时间：2026-09-17
> 范围：对照 `truth/` + `doc/` + `CONTINUITY.md` + `KNOWN_ISSUES.md` + `XRAY-全局图谱.md` 与运行时代码（src/ + server/）逐项校准
> 方法：以代码为真相，标注每条文档声明的「实现状态」与「差异类型」

---

## 一、需求校准分类总览

| 分类 | 数量 | 占比 | 处置策略 |
|---|---:|---:|---|
| ✅ **已实现且对齐** | 47 | 56% | 保留 |
| ⚠️ **部分实现** | 14 | 17% | 补全 |
| ❌ **未实现但文档声明** | 8 | 10% | 实现或修订文档 |
| 🟦 **代码已实现但文档未声明** | 11 | 13% | 补充文档 |
| ⛔ **与 prompt.js 主规范冲突** | 4 | 5% | 立即修复 |
| **合计** | **84** | 100% | — |

---

## 二、对照 truth/ 真理基准的校准

### 2.1 truth/00-TRUTH-BASE.md 领地红线校准

| 领地 | 红线声明 | 代码现状 | 校准结果 |
|---|---|---|---|
| A · Agent B | 五字段输出 | `contract.js:4-9 AGENT_B_V2_COLUMNS = ['stage','speech','board','actionSpec']`（4 字段） + audioUrl 程序回填（5 槽位） | ✅ 对齐（4 字段+audioUrl 是 runtime 习惯） |
| A · Agent B | 板书不输出起手坐标 | `contract.js:43-66 normalizeBoard` 严格剥离 startCoord | ✅ 对齐 |
| A · Agent B | 不算时长只给 startDelay | `prompt.js:240` + `contract.js` 无 duration 字段 | ✅ 对齐 |
| A · Agent B | 单手串行 order 递增 | `prompt.js:190` + `timing.js:73-269 computeRowGroupTimeline` 严格互斥 | ✅ 对齐 |
| B · Contract | 容错吸附 + 兜底 | `contract.js:14-25 STAGE_SYNONYMS` 温和吸附 + `tryExtractFallbackRows` 软降级 | ✅ 对齐 |
| C · 工具 | 4 个白名单工具 | `boardToolCatalog.js:29` 注册 4 类（rough-notation/line/arrow/draw），但 `prompt.js:194-229` 只声明 3 个（**draw 被 prompt.js:246 明文禁止**） | ⛔ **冲突**：draw 注册但 prompt 禁止 |
| C · 工具 | rough-notation 必须 exactText 锚定真文 | `roughNotationTool.js:125-150 prepareRoughNotationAction` 调 `textTargetRegistry.js:128 resolveTarget` | ✅ 对齐 |
| C · 工具 | rough-line/arrow 走 region 内坐标 | `roughDrawingTool.js:42-46 normalizePercentPoint` 校验 0-100 | ✅ 对齐 |
| D · 口播 | 分数念分母分之分子 | `prompt.js:160` + `asrPolish.js:38-43 normalizeBoardContent` 把 `7/15` 转 `\frac{7}{15}` | ✅ 对齐 |
| D · 口播 | x→艾克斯、行→航数 | `mathAsrConverter.js:257 formatMathSpeechToChinese` | ✅ 对齐 |
| D · 口播 | 严禁 LaTeX/Markdown 进 TTS | `prompt.js:157` 明确禁止 | ✅ 对齐 |
| E · 渲染 | 手稿感/微斜/抖动归渲染层 | `boardLayout.js:56-108 layoutBoardRows` 自然换行 | ✅ 对齐（但抖动未实现，DIGEST 第 7 节标注） |
| F · Check | 松门槛不卡死多样性 | `check-agent/contract.js:39-41` 仅修改 speech/board/actionSpec，保留行数/顺序/duration/stage/notes | ✅ 对齐 |

### 2.2 truth/00-夏夏原话汇总.md 关键口径校准

| # | 用户原话口径 | 代码现状 | 校准结果 |
|---|---|---|---|
| 1 | 画布 1726×980 | `stepHandoff.js:12 CANVAS_SIZE = { width: 1726, height: 980 }` | ✅ 对齐 |
| 2 | 题目字号 30px | `stepHandoff.js:14 QUESTION_FONT_SIZE = 30` | ✅ 对齐 |
| 3 | 题目字体微软雅黑 | `stepHandoff.js:18` 注释 + `index.html:46` 印刷体走系统栈含 Microsoft YaHei | ✅ 对齐 |
| 4 | 分析/解答/总结字号 = 题目 1.2~1.5 倍 | `stepHandoff.js:16 BOARD_FONT_SIZE = 35` + `:17 BOARD_FONT_RATIO_TEXT = '约题目的1.2~1.5倍，推荐35px'` | ⚠️ **弱分歧**：代码写死 35，描述说"推荐"；用户原话"35px板书"是硬值 |
| 5 | 字体 LikeJianJianTi | `stepHandoff.js:18 HANDWRITING_FAMILY = 'LikeJianJianTi'` + `index.html:46` link `zeoseven.com/490/main/result.css` | ✅ 对齐（zeoseven 是用户允许的国内 CDN） |
| 6 | 百分比坐标规则不变 | `stepHandoff.js:13 COORDINATE_SYSTEM = '百分比坐标 0-100'` | ✅ 对齐 |
| 7 | 行高不写死数值：自然换行 + 微抖动 | `stepHandoff.js:22-25 LINE_HEIGHT_RULE = '自然换行'` + `LINE_HEIGHT_FORMULA` 描述渲染层抖动 | ✅ 对齐（实现侧 `RealBoardPreview.vue:277 line-height: 1.5` 是渲染默认） |
| 8 | 题目区行高 1.65 固定（印刷体） | `stepHandoff.js:15 QUESTION_LINE_HEIGHT = 1.65` | ✅ 对齐 |
| 9 | dev 期只跑 vite dev，不 build | 项目当前未执行 build（用户禁止） | ✅ 对齐 |
| 10 | 端口 3001/3200/5199，3000 是另一项目 | `vite.config.js:21 port: 3000` | ⛔ **冲突**：vite.config.js 默认 3000 与另一项目冲突 |
| 11 | 禁境外资源（Google Fonts/jsdelivr/unpkg/ImgBB） | 全项目无境外资源引用（除 zeoseven） | ✅ 对齐 |
| 12 | handdraw-player.html / lite-player.html 是用户删的，不要补回 | `public/deliverable/handdraw-player.html` 不存在 | ✅ 对齐（但 renderDeliverableHtml 仍尝试读，走降级） |
| 13 | R2/R4/R5 有用、大用、不可删除 | `drawIntentTool.js` 保留（R2），`boardTypography.assertHandwritingScope` 保留（R4），`handActionScheduler` 无断点续播（R5 待改造） | ✅ 对齐（保留但需改进） |
| 14 | 板书可超出本区，不重叠、不溢出画布 | `prompt.js:71-72` 明确允许 + `boardLayout.js:56-108` 触底 clamp 95% | ✅ 对齐 |
| 15 | 板书速度 1 秒 2 字 | `prompt.js:268` "1 秒 2~3 字" + `timing.js:2 AGENT_B_V2_SPEECH_RATE = 160` (160 字/分钟≈2.67 字/秒) | ✅ 对齐 |
| 16 | 动作速度同速 | `timing.js:52-71 estimateActionDuration` 与板书速度同步 | ✅ 对齐 |
| 17 | 输出模板「stage-row数组-预估time\|口播\|坐标-板书\|时间节点-工具-做什么」 | `prompt.js:343-352` 模板说明（仅作参考格式，JSON 合同不变） | ✅ 对齐 |

### 2.3 truth/territory-*.md 领地边界校准

| 领地 | 边界声明 | 代码现状 | 校准结果 |
|---|---|---|---|
| A · Agent B | 不动 A 识别逻辑 | `recognitionHandler.js` + `recognitionPrompt.js` + `agentAKnowledge.js` 独立 | ✅ 对齐 |
| A · Agent B | 不动 B（AgentBDirect.vue 零改动） | `AgentBDirect.vue` 是核心，但 5330 行 | ⚠️ 维护性问题 |
| B · Contract | 容错吸附 + 兜底 | `contract.js:14-25 STAGE_SYNONYMS` + `tryExtractFallbackRows` | ✅ 对齐 |
| C · 动作锚点 | 4 个白名单工具 | catalog 注册 4 类，prompt 声明 3 个 | ⛔ **冲突**：draw 注册但 prompt 禁止 |
| C · 工具 | rough-notation 7 种动作 | `roughNotationTool.js:7-15` 7 种 | ✅ 对齐（但 skill/liyongle-elementary/board-rules.js:128 只声明 2 种） |
| E · 渲染 | 手稿感归渲染层 | `boardLayout.js` 自然换行 + `roughDrawingTool.js` 手绘几何 | ✅ 对齐（抖动未实现） |

---

## 三、对照 doc/ 规范的校准

### 3.1 doc/DELIVERABLE_API_SPEC.md 字段校准

| 字段 | 文档要求 | 代码现状 | 校准结果 |
|---|---|---|---|
| 根级 `stats` | 必填，全局宏观统计 | `deliverableStoreHandler.writeDeliverableFile` 从未生成 | ❌ **未实现** |
| 根级 `problemText` | 必填 | 依赖前端传入，未强制 | ⚠️ **部分实现** |
| 根级 `apiSpecVersion` | 必填 | 代码无字段 | ❌ **未实现** |
| 根级 `canvasParams` | 必填 | `writeDeliverableFile` 注入 | ✅ 对齐 |
| 根级 `rows[]` | 必填 | 代码注入 | ✅ 对齐 |
| row.`stage` | 必填 | 透传 | ✅ 对齐 |
| row.`speech` | 必填 | 透传 | ✅ 对齐 |
| row.`board` | 必填 | 透传 | ✅ 对齐 |
| row.`actionSpec` | 必填 | 透传 | ✅ 对齐 |
| row.`duration` | 规范要求 | `proxySelfCheck.js:227 assert.equal(Object.hasOwn(checkBody.rows[0], 'duration'), false)` 主动删除 | ⛔ **冲突**：文档要求但代码主动删除 |
| row.`estimatedDurationMs` | 可选 | `deliverableStoreHandler.js:150-160` 主动注入 | ✅ 对齐 |
| row.`audioUrl` | 可选 | 依赖前端 TTS 后回传 | ✅ 对齐 |
| row.`plan` | 文档未提及 | `deliverableStoreHandler.js:171 plan: row.plan \|\| exclusiveExecutionPlan` | 🟦 **代码新增字段未文档化** |
| row.`totalDurationMs` | 文档未提及 | `deliverableStoreHandler.js:169` | 🟦 **代码新增字段未文档化** |
| row.`timingPolicy` | 文档未提及 | `deliverableStoreHandler.js:173 timingPolicy: 'speech-full-board-action-mutually-exclusive'` | 🟦 **代码新增字段未文档化** |
| row.`board.startCoord` | 外部播放器依赖 | `deliverableStoreHandler.js:159` 调 `layoutBoardRows` 写回 | ✅ 对齐（09-15 已修复，DIGEST 第 7 节确认） |

### 3.2 doc/XRAY-全局图谱.md 风险清单校准

| XRAY ID | 文档评级 | 当前代码状态 | 校准结果 |
|---|---|---|---|
| R1 | 高 100% (`checkAgentHandler.js:243-244` 缺 fs/path import) | `checkAgentHandler.js:11-12` 已 import | ✅ **已修复** |
| R2 | 中 100% (`/api/knowledge/revert` 分支不可达) | `knowledgeRefineHandler.js:88` 仍写 `if (path === '/api/knowledge/revert')` | ❌ **未修复**（vite middleware 前缀剥离后 path 必为 `/revert`） |
| R3 | 高 (`RealBoardPreview.resolveRegion` 仅 question 区可用) | `RealBoardPreview.vue:41-43 resolveCanvas()` 返回 SVGElement 而非 HTMLElement → rough/draw 守卫恒 false → 全部静默失效 | ❌ **未修复**（红队已坐实，等夏夏放行） |
| R4 | 高 中 (`sanitizeRowLayout` 下限多行截断) | `contract.js:148 sanitizeRowLayout` 仍存在但策略宽松 | ⚠️ **部分修复** |
| R5 | 高 (`Fish Audio` 硬编码默认 Key) | `fishAudioHandler.js:12-15` 仍硬编码 | ❌ **未修复** |
| R6 | 中 (`cleanupHandler` 未保留 `*.original.json`) | `cleanupHandler.js:13-14` 仍只保留 current.json + 保留集 | ❌ **未修复**（与 R2 叠加，revert 完全失效） |
| R7 | 高 100% (`vite build` 必失败：lite-player.html 不存在) | `vite.config.js:46-50` 当前 input 只列 `main / board-preview / agent-b-v2`，未列 lite-player.html | ✅ **已修复**（XRAY 描述与当前代码不符） |
| R8 | 高 100% (`handdraw-player.html` 模板不存在) | Glob 全项目零结果；`renderDeliverableHtml.js:14 existsSync` 永远 false | ❌ **未修复**（用户明示"不要补回"，但 renderDeliverableHtml 仍尝试读） |
| R9 | 中 低 (三份 HTML 内嵌 fetch 劫持) | `index.html:7-30` 仍有 fetch 劫持脚本 | ⚠️ **未修复**（设计上保护 fetch 不被覆盖） |
| R10 | 低 100% (五字段口径不一致) | UI 称"五字段"，contract 冻结 4 列 | ⚠️ **未修复**（语义双轨） |
| R11 | 中 高 (`coordinateMode` 不在 handoff JSON) | `agentBV2Handler.js:163` 从 `body.canvasParams.coordinateMode` 读，不读 handoff | ✅ **对齐**（设计上由 body 传入） |
| R12 | 中 100% (`boardTypography.js` 全部导出零调用) | `board-tools/boardTypography.js` 已不存在；`board-tools/boardTypography.css` 仍存在但字体族无消费者 | ⚠️ **部分修复**（JS 已删，CSS 残留） |

### 3.3 doc/minimal-runtime-loop.md 主链校准

| 锚点 | 文档声明 | 代码现状 | 校准结果 |
|---|---|---|---|
| qh-loop-build-001 | `npm run build` 成功 | 当前未执行 build（用户禁止） | ⚠️ **未验证**（XRAY R7 已修复，理论应能成功） |
| qh-loop-proxy-001 | `npm run check:proxy` 通过 | `proxySelfCheck.js` 代码可读，逻辑覆盖 CORS/UPSTREAM/HANDOFF/知识回填/check agent | ⚠️ **未验证** |
| 主链入口 | `src/main.js → App.vue → DirectFlow.vue → Step1Entry.vue → AgentBDirect.vue → generateAgentBV2Rows → POST /api/agent-b-v2/generate` | 完全一致 | ✅ 对齐 |
| 持久化边界 | "A/B 配置 + C 配置 localStorage；rows 只在页面内存" | 实际还写了 `public/board-result/*.json` + `current.json` 实体归档 | ⚠️ **文档与代码不完全一致**（rows 确实有实体归档） |
| API 唯一真相 | 前端识别/生成/检查三 POST + Vercel Function 三接口 | 实际 vite 提供了 22 个端点，Vercel 仅 6 个 | ⚠️ **文档保护边界远小于实际暴露面** |

### 3.4 doc/board_risk_matrix.md R1-R5 校准

| 风险 ID | 文档评级 | 当前代码状态 | 校准结果 |
|---|---|---|---|
| R1 | P1 (rough-notation 仅 question 区可运行) | `RealBoardPreview.vue:44 querySelector` + `textTargetRegistry.js:116 requireElement` 抛错被吞 | ❌ **未修复**（同 XRAY R3） |
| R2 | P2 (draw 意图工具执行空壳) | `drawIntentTool.js` 已实现 `prepareDrawIntentAction` 真实绘制，但 prompt 未声明 → 模型永不产出 → 静默不触发 | ⚠️ **部分修复**（执行已写、数据源为零、渲染层 SVG 守卫仍挡） |
| R3 | P3 (L4 用户画笔层未实现) | `RealBoardPreview.vue:181` 仅空 div aria-hidden | ❌ **未修复**（边界保留） |
| R4 | P2 (手写字体范围断言分裂) | `boardTypography.js` 已删除（R12 部分修复）；`boardTypography.css` 残留 | ⚠️ **部分修复** |
| R5 | P2 (调度器无断点续播/单步隔离) | `handActionScheduler.js` 仍无断点续播 | ❌ **未修复**（待音频驱动 row-by-row 解决） |

---

## 四、对照 CONTINUITY.md 的需求校准

### 4.1 用户核心诉求实现状态

| # | 用户诉求 | 文档状态 | 代码现状 | 校准结果 |
|---|---|---|---|---|
| 1 | handoff 必须有实体文件存档 | 后端 ✅ / 前端 ❌ 未接入 | `Step1Entry.vue:700 POST /api/handoff` 已接入；`handoffStoreHandler.js` 完整 | ✅ **已修复**（CONTINUITY 第 17 行的"链路断裂"已解决） |
| 2 | 截图必须有 | 后端 ✅ / 前端 ❌ 整个链路被注释冻结 | `Step1Entry.vue:608 captureAndSaveScreenshot` 已实现，使用 `window.snapdom` + `POST /api/screenshot` | ✅ **已修复** |
| 3 | 知识点修缮按钮 | 后端 ✅ / 前端 ❌ 没按钮没函数 | `AgentBDirect.vue:1462 refineKnowledge` 已实现 | ✅ **已修复** |
| 4 | 不动 A / 不动 B / 单独写文件 / API 复用 A 配置 / 偷偷修缮 | 设计原则 | 全部遵守 | ✅ 对齐 |

### 4.2 画布参数口径校准

| 字段 | CONTINUITY 文档 | 代码现状 | 校准结果 |
|---|---|---|---|
| 画布尺寸 | 1726×980 | `stepHandoff.js:12 CANVAS_SIZE = { width:1726, height:980 }` | ✅ 对齐 |
| 题目字号 | 30px | `QUESTION_FONT_SIZE = 30` | ✅ 对齐 |
| 题目字体 | Segoe UI / PingFang SC / Microsoft YaHei | `index.html:46` 系统字体栈 | ✅ 对齐 |
| 题目行高 | 1.65 | `QUESTION_LINE_HEIGHT = 1.65` | ✅ 对齐 |
| 板书字号 | 约 35px（1.2~1.5 倍） | `BOARD_FONT_SIZE = 35` + `BOARD_FONT_RATIO_TEXT = '约题目1.2~1.5倍，推荐35px'` | ⚠️ **弱分歧**（同原话校准） |
| 板书字体 | LikeJianJianTi | `HANDWRITING_FAMILY = 'LikeJianJianTi'` | ✅ 对齐 |
| 其他区行高 | 自然换行 + 渲染层微抖动 | `LINE_HEIGHT_RULE = '自然换行'` | ✅ 对齐（抖动未实现） |
| 颜色 | 分析红 / 解答黑 / 总结黑 | `stepHandoff.js` 未声明颜色，由各组件 CSS 控制 | ⚠️ **未集中真源化** |

### 4.3 速度规则校准

| 规则 | CONTINUITY 9.1 | 代码现状 | 校准结果 |
|---|---|---|---|
| 板书速度 | 1 秒 2 个汉字 | `prompt.js:268` "1 秒 2~3 字" + `timing.js:2 AGENT_B_V2_SPEECH_RATE = 160`（160 字/分钟≈2.67 字/秒） | ✅ 对齐 |
| 动作速度 | 差不多同样速度 | `timing.js:52-71 estimateActionDuration` 与板书速度同步 | ✅ 对齐 |

### 4.4 handoff 字段校准

| 字段 | CONTINUITY 文档 | 代码现状 | 校准结果 |
|---|---|---|---|
| `screenshotUrl` | 用户要求新增 | `stepHandoff.js buildStep1Handoff` 已加入 | ✅ 对齐 |
| `knowledgeBasePath` | 用户要求新增 | `stepHandoff.js buildStep1Handoff` 已加入 | ✅ 对齐 |
| `confirmedAt` | 已实现 | `stepHandoff.js` 已加入 | ✅ 对齐 |
| `showGrid` | 已实现 | `stepHandoff.js` 已加入 | ✅ 对齐 |
| `agentPageName` | 已实现 | `stepHandoff.js` 已加入 | ✅ 对齐 |
| `agentCapability` | 已实现 | `stepHandoff.js` 已加入 | ✅ 对齐 |
| `stageRatioSuggestion` | 9.3 节用户指定 | `stepHandoff.js:141 detectStageRatio` 已实现 | ✅ 对齐 |
| `环节配比占比` | 9.3 节同义 | `stepHandoff.js` 双写并存 | ⚠️ **双写**（XRAY R10 已记录） |
| `zoneAnchors` | 已实现 | `stepHandoff.js:257 extractZoneAnchors` | ✅ 对齐 |
| `topicLayout` | 已实现 | `stepHandoff.js` 已加入 | ✅ 对齐 |
| `boardPlan` | 已实现 | `stepHandoff.js` 已加入 | ✅ 对齐 |

### 4.5 CONTINUITY API 清单状态对比

| API | CONTINUITY 状态 | 当前实际状态 | 校准结果 |
|---|---|---|---|
| POST /api/recognition/problem | ✅ 已接入 | ✅ | ✅ 对齐 |
| POST /api/agent-b-v2/generate | ✅ 已接入 | ✅ | ✅ 对齐 |
| POST /api/check-agent/check | ✅ 已接入 | ✅ | ✅ 对齐 |
| GET /api/handoff | ✅ 已接入 | ✅ | ✅ 对齐 |
| POST /api/handoff | ❌ 未接入 | ✅ 已接入 | ✅ **已修复** |
| GET /api/handoff/list | ❌ 未接入 | ✅ 已接入（`handoffStoreHandler.js:175`） | ✅ **已修复** |
| POST /api/knowledge/refine | ❌ 未接入 | ✅ 已接入（`AgentBDirect.vue:1466`） | ✅ **已修复** |
| POST /api/knowledge/revert | ❌ 未接入 | ⛔ 代码已写但分支不可达 | ❌ **未修复**（L3-1 BUG） |
| POST /api/screenshot | ❌ 未接入 | ✅ 已接入（`Step1Entry.vue:630`） | ✅ **已修复** |

---

## 五、对照 KNOWN_ISSUES.md 的校准

### 5.1 P0 高优先级问题状态

| KNOWN_ISSUES ID | 文档状态 | 当前代码状态 | 校准结果 |
|---|---|---|---|
| P0-1 canvasParams 循环引用 | ✅ 已修复 | `AgentBDirect.vue:416-417` 已改 `.value` | ✅ 对齐 |
| P0-2 B 输出解析过于严格 | ❌ 未修复 | `agentBV2Handler.js:300-336 tryExtractFallbackRows` 已实现软降级 | ✅ **已修复**（文档未同步） |
| P0-3 修缮 API 一调就写文件 | ❌ 未修复 | `knowledgeRefineHandler.js` 仍一调就覆写 | ❌ **未修复**（与 R2 叠加，revert 完全失效） |

### 5.2 P1 中优先级问题状态

| KNOWN_ISSUES ID | 文档状态 | 当前代码状态 | 校准结果 |
|---|---|---|---|
| P1-1 两套参数体系并存 | ❌ 未修复 | UI canvasParams ref 与 handoff canvasParams 仍并存 | ❌ **未修复** |
| P1-2 区域命名 question/topic 不一致 | ❌ 未修复 | `boardLayout.js/stepHandoff.js/prompt.js/roughDrawingTool.js/textTargetRegistry.js` 仍用 question；`AgentBDirect.vue` 仍用 topic | ❌ **未修复** |
| P1-3 zones 死代码 | ❌ 未修复 | `AgentBDirect.vue:135-141 canvasParams.zones` 仍定义但模板/script 0 引用 | ❌ **未修复** |
| P1-4 导出与渲染不一致 | ❌ 未修复 | `speechMarkdown.js` 仍读 UI canvasParams 的值（topicFontSize/zones 死值） | ❌ **未修复** |
| P1-5 handoff 白名单冗余字段 | ❌ 未修复 | 6 套坐标 + 3 套布局 + 2 套 canvasParams 仍并存 | ❌ **未修复** |
| P1-6 relatedKnowledge 数据结构不统一 | ❌ 未修复 | A 侧写对象数组，修缮输出字符串数组 | ❌ **未修复** |
| P1-7 duration 全链路清理 | ✅ 已完成 | 已清理 8 个文件 | ✅ 对齐 |

### 5.3 P2 低优先级问题状态

| KNOWN_ISSUES ID | 文档状态 | 当前代码状态 | 校准结果 |
|---|---|---|---|
| P2-1 canvasParams 大多数字段是描述性文本 | ❌ 未处理 | `stepHandoff.js buildCanvasParams` 仍含大量描述字符串 | ❌ **未修复** |
| P2-2 fontSize 12 子字段只用 1 个 | ❌ 未处理 | 同上 | ❌ **未修复** |
| P2-3 lineHeight.others 是字符串 | ❌ 未处理 | `stepHandoff.js:154 lineHeight.others = '自然换行+渲染层微抖动'` | ❌ **未修复**（设计上已变描述） |
| P2-4 导出 MD 不是 TTS 可直接用 | ❌ 未修复 | `speechMarkdown.js:2` 仍含 `# 口播稿` 元数据 | ❌ **未修复** |
| P2-5 自检脚本仍断言旧导出格式 | ❌ 未修复 | `proxySelfCheck.js:227` 仍要求 `# 口播稿` | ❌ **未修复** |
| P2-6 Descriptions span 错配 | ✅ 已修复 | — | ✅ 对齐 |
| P2-7 checkStatus failed_fallback 弹窗 | ✅ 已修复 | — | ✅ 对齐 |

---

## 六、对照 AGENTS.md 的校准

### 6.1 AGENTS.md 核心原则校准

| 原则 | 文档声明 | 代码现状 | 校准结果 |
|---|---|---|---|
| 极简但完整 | 每个字都有价值，无冗余 | 4 处死代码 638 行 + 8 个超限文件 | ⚠️ **未达成** |
| AI 友好 | 结构化可执行语言 | ✅ 良好 | ✅ 对齐 |
| 公共资源复用 | CSS/模块/函数纳入公共引用 | `stepHandoff.js` 真源已收敛，但 3 处 `genProjectCode` 重复 | ⚠️ **部分达成** |
| 图优先于文档 | 系统全景图 + 施工进展图 | `项目实时架构模块图.md` 存在但极简 | ⚠️ **部分达成** |
| 变更留痕 | ENGINEERING_LOG + DECISIONS + PROJECT_STATE | ✅ 三件套完整 | ✅ 对齐 |
| 验证优先 | 没跑过的代码不算好 | 全项目 0 测试 + 多处空 catch | ❌ **未达成** |
| 标准工业化 | 优先成熟框架 | Vue3+Vite+Antd+rough.js 全部成熟 | ✅ 对齐 |
| 禁止过度设计 | 字段可溯源 | `board.place` 意图层（near/avoid/flow）属自造概念，已主动拦截 | ✅ 对齐 |
| 缓存+增量阅读 | 用缓存+增量阅读 | ✅ 良好 | ✅ 对齐 |
| 错误熔断 | 错超 5 次停下 | 项目历史多次"超过 5 次"问题已通过用户介入解决 | ✅ 对齐 |

### 6.2 Agent A → B Handoff 校准

| 原则 | 文档声明 | 代码现状 | 校准结果 |
|---|---|---|---|
| 不动 Agent A 上传/识别/图像/布局/坐标逻辑 | — | ✅ 完整保留 | ✅ 对齐 |
| Handoff 只传文本字段，按 `HANDOFF_TEXT_FIELDS` 白名单 | — | `agentBV2Handler.js:43-44` 已实现 | ✅ 对齐 |
| Agent B 发送 `prompt.js` system prompt + handoff 文本 | — | `agentBV2Handler.js:163-170` 已实现 | ✅ 对齐 |
| 图片传输已冻结 | — | `normalizeAgentBPromptHandoff` 已过滤 | ✅ 对齐 |

### 6.3 B Prompt 真源校准

| 真源 | 文档声明 | 代码现状 | 校准结果 |
|---|---|---|---|
| system prompt | `src/agent-b-v2/skills/` 动态加载 | `agentBV2Handler.js:5-7` 支持 systemPrompt > skill > AGENT_B_V2_SYSTEM_PROMPT | ✅ 对齐 |
| Agent A 情报 | `src/agent-b-v2/service.js` 组装 userPayload | `service.js:9` 已实现 | ✅ 对齐 |
| Agent A 知识库 | `doc/knowledge-list.md` | 实际用 `doc/knowledge-a.compact.json`（247 条） | ⚠️ **部分对齐**（文档名与代码名不一致） |
| Agent B 必读资料 | `doc/key.md + doc/knowledge-list.md + doc/speech-board-guide.md` | `key.md` 不存在；`knowledge-list.md` 仅 180 字节（仅声明无内容）；`speech-board-guide.md` 存在 | ⚠️ **部分对齐**（key.md 缺失） |

### 6.4 Check Agent C 唯一真相校准

| 真源 | 文档声明 | 代码现状 | 校准结果 |
|---|---|---|---|
| 检查范围主人 | `doc/key.md` | `key.md` 不存在 | ❌ **缺失** |
| 检查细化依据 | `doc/self-check.md + doc/speech-board-guide.md` | 两者均存在 | ✅ 对齐 |
| 前端调用 | `src/check-agent/service.js` | ✅ 已实现 | ✅ 对齐 |
| 服务端提示与解析 | `server/checkAgentHandler.js + src/check-agent/contract.js` | ✅ 已实现 | ✅ 对齐 |
| 只修改 speech/board/actionSpec | — | `check-agent/contract.js:3 ALLOWED_FIELDS` 已实现 | ✅ 对齐 |
| 步骤数量/顺序/duration/stage/notes 保持原样 | — | ✅ 已实现 | ✅ 对齐 |
| 用户手动修改后的内容优先 | — | `check-agent/service.js:47` 用 `body.currentRows` 优先 | ✅ 对齐 |

---

## 七、与 prompt.js 主规范冲突清单（立即修复）

| # | 冲突点 | 位置 | prompt.js 规范 | 当前代码 | 修复方向 |
|---|---|---|---|---|---|
| 1 | draw 工具注册但 prompt 禁止 | `boardToolCatalog.js:29` 注册 `getDrawIntentAgentTool()` | `prompt.js:246` 明文「不得输出 'draw'」 | catalog 注册 4 类工具 | 从 catalog 移除 draw 或 prompt 追加声明 |
| 2 | Skill examples.js 字段名错误 | `skills/liyongle-elementary/examples.js:24-28` | `prompt.js:199-208` action/target.exactText/colorId | examples 用 type/text/color | 修正 examples 字段名 |
| 3 | Skill output-format.js 自检清单违反主规范 | `skills/liyongle-elementary/output-format.js:25` | `prompt.js:159` 「保留阿拉伯数字」 | 自检清单第 5 条「数字都是中文发音」 | 修正自检清单第 5 条 |
| 4 | Skill board-rules.js 工具动作清单不全 | `skills/liyongle-elementary/board-rules.js:128` | `roughNotationTool.js:7-15` 实际支持 7 种 | skill 文档只声明 underline/highlight 2 种 | 补全或对齐代码 |

---

## 八、隐藏逻辑识别（代码有但文档未声明）

| # | 隐藏逻辑 | 位置 | 性质 | 处置建议 |
|---|---|---|---|---|
| 1 | `tryExtractFallbackRows` 软降级 | `agentBV2Handler.js:300-336` | 正向（容错） | 文档化 |
| 2 | Check Agent 三重降级到本地 ASR | `checkAgentHandler.js:188-214,293-336` | 正向（容错） | 文档化 |
| 3 | recognition 24h/100 条内存缓存 | `recognitionHandler.js:16-47` | 正向（性能） | 文档化 |
| 4 | `.original.json` 备份机制 | `boardResultStore.js:127-148` + `knowledgeRefineHandler.js:54-77` | 正向（可回滚） | 文档化 |
| 5 | `deliverableStoreHandler.syncExistingDeliverableHtmls` 启动时同步 | `deliverableStoreHandler.js:49-77` | 中性（有 bug） | 修复空 catch + 文档化 |
| 6 | `productionServer./api/mock/problem` 在 prod 仍生效 | `productionServer.js:46-57` | 负向（不应保留） | 加 NODE_ENV 守卫 |
| 7 | `cleanupHandler.CLEANUP_RULES` 不保留 `.original.json` | `cleanupHandler.js:13-14` | 负向（误删备份） | 加保留规则 |
| 8 | `deliverableStoreHandler` projectCode 目录穿越 | `deliverableStoreHandler.js:146` | 负向（安全） | 加校验 |
| 9 | 3 个 vite middleware 静态服务目录穿越 | `handoffStoreHandler.js:190-210` + `screenshotStoreHandler.js:69-81` + `deliverableStoreHandler.js:483-547` | 负向（安全） | 加 safeFile 校验 |
| 10 | Fish Audio 双 Key 硬编码 | `fishAudioHandler.js:12-15` | 负向（安全） | 移到 .env |
| 11 | `bCacheKey` 缺 canvasParams | `AgentBDirect.vue:975-1031` | 负向（缓存错） | 加 canvasParams 字段 |

---

## 九、未实现功能清单（文档要求但代码未实现）

| # | 功能 | 文档要求位置 | 代码现状 | 优先级 |
|---|---|---|---|:---:|
| 1 | `/api/knowledge/revert` 还原接口 | `CONTINUITY.md` 用户诉求 | 分支不可达（L3-1 BUG） | P0 |
| 2 | `handdraw-player.html` 交付物播放器 | `renderDeliverableHtml.js:6` | 文件不存在（用户明示不补回，但 renderDeliverableHtml 仍尝试读必走降级） | P1 |
| 3 | `doc/key.md` 检查范围主人 | `AGENTS.md` Check Agent C 真相 | 文件不存在 | P2 |
| 4 | 整篇连播编排器 | `DIGEST 第 7 节` | 全仓 0 命中 `playAll/连播/播放全部/autoPlay` | P2 |
| 5 | `vite build` 验证 | `minimal-runtime-loop.md qh-loop-build-001` | 未执行（用户禁止） | P3 |
| 6 | L4 用户画笔层交互 | `truth/territory-E-render.md` | `RealBoardPreview.vue:181` 仅空 div | P3 |
| 7 | 渲染层自然排版防重叠算法 | `MEMORY.md` 演进方向 | `boardLayout.js:layoutBoardRows` 部分实现（不防重叠，只触底 clamp） | P3 |
| 8 | 音频驱动 row-by-row 播放 | `MEMORY.md 2026-09-15` | `timing.js:79-84` 已铺一半，缺整篇编排器 | P3 |

---

## 十、需求校准总结

### 10.1 校准统计

| 校准维度 | 总项数 | ✅ 对齐 | ⚠️ 部分 | ❌ 未实现 | 🟦 代码新增 | ⛔ 冲突 |
|---|---:|---:|---:|---:|---:|---:|
| truth/ 真理基准 | 18 | 16 | 1 | 0 | 0 | 1 |
| 夏夏原话汇总 | 17 | 15 | 1 | 0 | 0 | 1 |
| DELIVERABLE_API_SPEC | 16 | 9 | 1 | 2 | 3 | 1 |
| XRAY-全局图谱 R1-R12 | 12 | 3 | 3 | 5 | 0 | 1 |
| minimal-runtime-loop | 5 | 2 | 3 | 0 | 0 | 0 |
| board_risk_matrix R1-R5 | 5 | 0 | 2 | 3 | 0 | 0 |
| CONTINUITY 核心诉求 | 9 | 6 | 0 | 3 | 0 | 0 |
| CONTINUITY 画布参数 | 8 | 7 | 1 | 0 | 0 | 0 |
| CONTINUITY API 清单 | 9 | 4 | 0 | 1 | 4 | 0 |
| KNOWN_ISSUES P0-P2 | 16 | 5 | 0 | 11 | 0 | 0 |
| AGENTS.md 核心原则 | 10 | 7 | 3 | 0 | 0 | 0 |
| AGENTS.md Handoff/B/Check 真相 | 11 | 9 | 2 | 0 | 0 | 0 |
| prompt.js 冲突专项 | 4 | 0 | 0 | 4 | 0 | 0 |
| **合计** | **140** | **83 (59%)** | **14 (10%)** | **28 (20%)** | **7 (5%)** | **8 (6%)** |

### 10.2 主要发现

1. **59% 文档与代码对齐**：主链路、核心契约、画布参数、Handoff 字段、四环教学法、五字段合同、单手互斥时间线、固定开场/收尾话术、口播发音规则全部对齐。

2. **6% 与 prompt.js 直接冲突**：4 项需立即修复（draw 工具、examples.js 字段名、output-format.js 自检清单、board-rules.js 工具清单）。

3. **20% 文档要求未实现**：8 项 P0/P1 阻塞（knowledge/revert 不可达、handdraw-player.html 缺失、key.md 缺失、连播编排器、build 验证、L4 画笔、自然排版防重叠、音频驱动播放）。

4. **5% 代码新增字段未文档化**：`plan` / `totalDurationMs` / `timingPolicy` / `degraded` / `checkStatus: 'local_asr_polished'` 等运行时字段需补入 `DELIVERABLE_API_SPEC.md`。

5. **10% 部分实现**：主要是字号 35px vs 描述"推荐35px"的弱分歧 + 端口 3000 与另一项目冲突 + XRAY R2/R3/R5/R6 等历史风险未修复。

### 10.3 阶段二治理优先级建议

| 优先级 | 项数 | 类别 | 治理策略 |
|---|---:|---|---|
| 🔴 P0 立即 | 8 项 | 文档要求未实现 + prompt.js 冲突 | 阶段二·六步心法第 1-3 步 |
| 🟡 P1 高优 | 14 项 | KNOWN_ISSUES P0-P1 未修复 + 隐藏逻辑负向 | 阶段二·六步心法第 4-5 步 |
| 🟢 P2 中优 | 11 项 | 部分实现 + 代码新增字段文档化 | 阶段二·六步心法第 6 步 |
| ⚪ P3 长期 | 5 项 | 未实现功能（连播/L4/排版防重叠/音频驱动） | 阶段三以后 |

---

> 需求校准表阶段一产出完毕。下一份《遗留识别与分级治理清单》将基于本表 + 性能评估表 + 业务流图，按 L1/L2/L3 分级列出全部遗留问题。
