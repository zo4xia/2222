# 03 · 全维度性能评估表

> 阶段一·深度扫描产出 ｜ 生成时间：2026-09-17
> 6 维度评估：性能 / 可用性 / 依赖复杂度 / 安全性 / 可维护性 / 兼容性
> 评分标准：A 优秀(无短板) / B 良好(有改进点) / C 合格(有显著问题) / D 不合格(阻断/风险)

---

## 一、6 维度综合评估表

| 维度 | 前端 | 后端 | 综合 | 关键短板 |
|---|:---:|:---:|:---:|---|
| **1. 性能** | C | B | **C** | AgentBDirect 5330 行单文件首屏解析慢；B 生成缓存键缺 canvasParams；超长行 LRU 未启用 |
| **2. 可用性** | B | C | **C** | handdraw-player.html 缺失 → 打开产物 404；knowledge/revert 不可达；pic 目录 content-type 错配 |
| **3. 依赖复杂度** | B | A | **B** | 6 个生产依赖全部有效（无未使用）；但 4 处死代码模块占用 320 行 |
| **4. 安全性** | C | D | **D** | Fish Audio 双 Key 硬编码；deliverableStoreHandler 目录穿越；3 个 vite middleware 静态服务无防护 |
| **5. 可维护性** | D | C | **D** | AgentBDirect 单文件 5330 行；4 个组件超 500 行；无任何测试；空 catch 满天飞 |
| **6. 兼容性** | B | D | **C** | Vercel Functions 覆盖率 27%；vite.config.js 端口 3000 与另一项目冲突；demo.html 多套口径并存 |

### 综合评级：**C（合格但有显著问题）**

- 主链路 ✅ 全部接通，能跑通核心业务
- 但有 **4 个 P0 阻塞** + **9 个 L3 重度问题** + **11 个 L2 中度问题** 待治理

---

## 二、性能评估详表

### 2.1 前端性能

| 检查项 | 现状 | 评级 | 证据 |
|---|---|:---:|---|
| 首屏 JS 体积 | Antd 全量注册（main.js `app.use(Antd)`），未按需 | C | `src/main.js:2` |
| 单文件解析耗时 | AgentBDirect.vue 5330 行 + 3360 行 template | C | `wc -l` 实测 |
| 缓存策略 | B 生成 24h localStorage 缓存，但 `bCacheKey` 缺 canvasParams/coordinateMode | C | `AgentBDirect.vue:975-1031` |
| 图片懒加载 | 无 | C | 全项目无 `loading="lazy"` |
| 代码分割 | 无动态 import | C | 全项目无 `import()` |
| 字体加载 | zeoseven CDN + onerror 降级到系统楷体栈 | B | `index.html:46-48` |
| KaTeX 渲染 | 本地化 + renderToString 同步阻塞主线程 | B | `utils/mathText.js:6` |
| Reactivity | `safeDeepClone` 用 JSON.parse(stringify) 规避循环引用 | B | `AgentBDirect.vue:51` |
| 防抖/节流 | 部分有（problemText watch）；拖拽无防抖 | B | `Step1Entry.vue` |

### 2.2 后端性能

| 检查项 | 现状 | 评级 | 证据 |
|---|---|:---:|---|
| 上游超时 | B(OpenAI) 300s / B(Anthropic) 90s / Check 25s / Recognition 60s / Refine 60s / TTS 无 | B | 各 handler |
| 重试策略 | 多 Key 轮询 + 401/403/408/429/5xx 自动重试 + 退避 | A | `http.js:122-171` |
| 缓存层 | Recognition 24h/100 条内存缓存；TTS MD5 去重缓存；B 生成前端 localStorage 24h | A | 多处 |
| 并发控制 | TTS batch 默认 concurrency=3 | B | `fishAudioHandler.js:356` |
| 大文件流式响应 | 无（全部 readJsonBody 一次性读） | C | `http.js:51` |
| 静态文件服务 | productionServer 用 `fs.readFile` 一次性读 | C | `productionServer.js:107-121` |
| 知识库加载 | `docReferences.js` 启动时同步读 140KB JSON | B | `server/docReferences.js:3-5` |
| 日志输出 | `console.warn` maskApiKey 仅重试时输出 | B | 多处 |

### 2.3 性能瓶颈点

| # | 瓶颈 | 影响 | 优先级 |
|---|---|---|:---:|
| P1 | AgentBDirect.vue 5330 行单文件首屏解析 | 首屏慢 200-500ms | P1 |
| P2 | B 生成缓存键缺 canvasParams → 用户改画布参数后命中旧 rows | 用户体验差，需 Shift 强制刷新 | P2 |
| P3 | KaTeX renderToString 同步阻塞主线程 | 题目渲染时短暂卡顿 | P3 |
| P4 | Antd 全量注册 | bundle 体积 +200KB | P3 |
| P5 | productionServer 静态文件无 ETag/Cache-Control | 重复下载 | P3 |
| P6 | docReferences 启动时同步读 140KB | 服务启动慢 100ms | P3 |

---

## 三、可用性评估详表

| 检查项 | 现状 | 评级 | 证据 |
|---|---|:---:|---|
| 健康检查 | `/api/health` 已实现 | A | `vite.config.js:24-32` |
| Mock 数据 | `/api/mock/problem` 已实现（⚠️ prod 不应保留） | C | `vite.config.js:34-47` |
| 错误处理 | Check Agent 三重降级 + B 生成软降级 | A | `checkAgentHandler.js` + `agentBV2Handler.js:300-336` |
| Loading 反馈 | 多阶段呼吸 Loading + 全局脉冲遮罩 | A | `ProcessLoadingModal.vue` + `GlobalPulseLoading.vue` |
| 用户引导 | 全程无引导（首次使用难上手） | C | — |
| 错误提示 | message.error/warning 已用，但部分空 catch 静默 | C | 多处 `catch {}` |
| 国际化 | 全中文，无 i18n 框架 | B | — |
| 移动端适配 | 仅桌面端 | C | `viewport` 配置存在但 UI 桌面优先 |
| 键盘快捷键 | 无 | C | — |

### 可用性阻塞点

| # | 阻塞 | 影响 | 评级 |
|---|---|---|:---:|
| U1 | handdraw-player.html 不存在 → 「打开 HTML 产物」按钮 404 | 用户无法预览交付物 | 🔴 P0 |
| U2 | `/api/knowledge/revert` 不可达 → 备份永远读不回 | 用户「还原」按钮失效 | 🔴 P0 |
| U3 | `/pic/audio-*.mp3` content-type 错配 → 浏览器无法播放音频副本 | 资源浪费 + 用户混淆 | 🟡 P1 |
| U4 | productionServer 仍保留 `/api/mock/problem` → 生产环境暴露 mock | 数据污染 | 🟡 P1 |
| U5 | vite.config.js port=3000 与另一项目冲突 | dev 启动失败需手动改端口 | 🟡 P1 |

---

## 四、依赖复杂度评估

### 4.1 生产依赖矩阵

| 依赖 | 版本 | 引用文件数 | 评级 | 备注 |
|---|---|:---:|:---:|---|
| `vue` | ^3.5.39 | 全部 .vue + 3 lib | A | 核心框架 |
| `ant-design-vue` | ^4.2.6 | 8 文件 | B | 全量注册，可按需优化 |
| `@ant-design/icons-vue` | ^7.0.1 | 3 文件 44 图标 | A | — |
| `katex` | ^0.18.4 | 4 文件 | A | 本地化完整 |
| `rough-notation` | ^0.5.1 | 1 文件 | A | 单一消费者 |
| `roughjs` | ^4.6.6 | 2 文件 | A | — |

### 4.2 死代码依赖

| 模块 | 行数 | 状态 | 建议 |
|---|---:|---|---|
| `src/utils/cdnLoader.js` | 197 | 0 外部消费者 | 移除或接入字体加载 |
| `src/lib/dualStore.js` | 67 | 0 外部消费者 | 移除 |
| `src/lib/simpleIndexedDb.js` | 56 | 仅 dualStore 调用 | 移除 |
| `src/board-tools/drawIntentTool.js` | 318 | prompt.js 禁止 → 永不触发 | 移除或接入 |
| `src/board-tools/boardToolTiming.js` | 1 | 仅导出常量 | 保留 |
| `src/board-tools/boardTypography.css` | 7 | 字体族无消费者 | 移除或接入 |
| `src/agent-b-v2/prompt-v3-draft.js` | (不存在) | 已删除 | — |

### 4.3 devDependencies

| 依赖 | 版本 | 评级 | 备注 |
|---|---|:---:|---|
| `vite` | ^8.1.1 | A | 主构建工具 |
| `@vitejs/plugin-vue` | ^6.0.7 | A | — |
| `eslint` | ^10.8.1 | B | 配置允许空 catch（allowEmptyCatch:true）应关闭 |
| `eslint-plugin-vue` | ^10.10.0 | A | — |
| `@eslint/js` | ^10.0.1 | A | — |

### 4.4 第三方资源合规

| 资源 | 来源 | 状态 | 备注 |
|---|---|---|---|
| 手写字体 | `fontsapi.zeoseven.com/490/main/result.css` | ✅ 用户允许 | 唯一例外 |
| 印刷字体 | 系统字体栈（PingFang SC / Microsoft YaHei） | ✅ 本地 | `index.css:3` |
| KaTeX | `public/katex/*` 本地化 | ✅ | — |
| rough.js | `public/rough.js` 备份 + npm 包 | ⚠️ 冗余 | 实际用 npm 包 |
| snapdom.js | `public/snapdom.js` 本地化 | ✅ | — |
| 字体文件 | `public/fonts/pingfang-qiaomu.ttf` (3.8MB) | ⚠️ 字体族无人用 | 可移除 |

### 4.5 依赖复杂度评级：**B（良好）**

- 6 个生产依赖全部有效，无未使用依赖
- 但有 4 处死代码模块共 638 行占用维护成本
- 第三方资源全部本地化或符合用户铁律

---

## 五、安全性评估

### 5.1 安全风险清单

| # | 风险 | 严重度 | 位置 | 状态 |
|---|---|:---:|---|:---:|
| S1 | Fish Audio 双 Key 明文硬编码 | 🔴 高 | `fishAudioHandler.js:12-15` | 未修复 |
| S2 | deliverableStoreHandler projectCode 目录穿越（写入） | 🔴 高 | `deliverableStoreHandler.js:146` | 未修复 |
| S3 | deliverableStoreHandler id 参数目录穿越（读取） | 🔴 高 | `deliverableStoreHandler.js:86` | 未修复 |
| S4 | handoffStorePlugin 静态服务目录穿越（读取） | 🔴 高 | `handoffStoreHandler.js:190-210` | 未修复 |
| S5 | screenshotStorePlugin 静态服务目录穿越（读取） | 🔴 高 | `screenshotStoreHandler.js:69-81` | 未修复 |
| S6 | deliverableStorePlugin 静态服务目录穿越（读取） | 🔴 高 | `deliverableStoreHandler.js:483-547` | 未修复 |
| S7 | UPSTREAM_HOSTS 默认空 = 任意域名可被代理（SSRF） | 🔴 高 | `http.js:94-100` | 未修复 |
| S8 | Agent A/B/C API Key 明文写在 userApiConfig.js 默认值 | 🟡 中 | `userApiConfig.js:30-33` | 用户明示保留 |
| S9 | productionServer 6 个静态目录无鉴权 | 🟡 中 | `productionServer.js:107` | 未修复 |
| S10 | productionServer.mock/problem 在 prod 仍生效 | 🟡 中 | `productionServer.js:46-57` | 未修复 |
| S11 | API key 拼入 sha256 cacheKey | 🟢 低 | `recognitionHandler.js:25` | 不泄露但不必要 |
| S12 | 空 catch 满天飞吞异常（11+ 处） | 🟢 低 | 多处 | eslint 主动允许 |

### 5.2 CORS / UPSTREAM 配置审计

| 配置 | 默认值 | 行为 | 评级 |
|---|---|---|:---:|
| `CORS_ORIGINS` | `''` | `applyCors` 在 `allowedOrigins.length === 0` 时不设置 CORS 头 → **完全不开放跨域** | A |
| `UPSTREAM_HOSTS` | `''` | `resolveUserCredentials` 在 `allowedHosts.length === 0` 时**完全放行所有 host** → **任意域名可被代理** | D |

**严重不一致**：CORS 默认紧、UPSTREAM 默认松。

### 5.3 API Key 处理审计

| 处理点 | 行为 | 评级 |
|---|---|:---:|
| http.js maskApiKey | `${str.slice(0,4)}...${str.slice(-4)}` | A |
| http.js console.warn 重试时输出 maskApiKey | OK | A |
| fishAudioHandler DEFAULT_API_KEYS 明文 | 🔴 严重 | D |
| /api/tts/info 响应 keys.map(maskKey) | OK | A |
| 各 handler 响应不回显 apiKey | OK | A |
| recognitionHandler cacheKey 拼入 apiKey sha256 | 不泄露但不必要 | B |

### 5.4 安全性评级：**D（不合格）**

> 7 个 P0 安全漏洞，2 个 P1 安全风险。建议在阶段二治理时优先修复 S1-S7。

---

## 六、可维护性评估

### 6.1 文件超限统计

| 文件 | 行数 | 阈值 | 倍数 | 评级 |
|---|---:|---:|---:|:---:|
| `src/agent-b-v2/AgentBDirect.vue` | 5330 | 300 | 17.8x | D |
| `src/board-preview/BoardPreviewApp.vue` | 1961 | 300 | 6.5x | D |
| `src/components/Step1Entry.vue` | 1523 | 300 | 5.1x | D |
| `src/components/BoardContentLayer.vue` | 1015 | 300 | 3.4x | D |
| `src/components/VisualTimeline.vue` | 587 | 300 | 2.0x | C |
| `server/deliverableStoreHandler.js` | 551 | 500 | 1.1x | C |
| `src/components/ProcessLoadingModal.vue` | 514 | 300 | 1.7x | C |
| `src/components/GlobalPulseLoading.vue` | 404 | 300 | 1.3x | C |

**8 个文件超限**，总超限行数 **11,885 行**。

### 6.2 测试覆盖率

| 类型 | 数量 | 覆盖范围 | 评级 |
|---|---|---|:---:|
| 单元测试 | 0 | — | D |
| 集成测试 | 0 | — | D |
| E2E 测试 | 0 | — | D |
| 自检脚本 | 2 | `proxySelfCheck.js`（mock fetch）+ `agentAKnowledgeSelfCheck.js`（3 case） | C |

**全项目零真实测试**，任何「复用后行为不变」的说法都无运行时证据。

### 6.3 代码质量

| 检查项 | 现状 | 评级 |
|---|---|:---:|
| ESLint 配置 | flat config，`allowEmptyCatch:true` 主动允许吞异常 | C |
| 代码风格 | 单引号 + 无分号 + 2 空格缩进（一致） | A |
| 命名规范 | 部分双轨（question/topic / 字段名 type/action） | C |
| 重复实现 | genProjectCode 3 处复制 / originalBackup 2 处复制 / sendJson 2 套签名 | C |
| 死代码 | 4 个模块共 638 行无消费者 | C |
| 注释残留 | 多处过期注释（如 roughDrawingTool.js:237 @orphan 已被调用） | C |
| 魔法数字 | 多处（30/20、2800ms、5步骤、6目录、1200ms、12345 seed 等） | C |
| 错误处理 | 11+ 处空 catch 静默吞异常 | D |

### 6.4 文档完整度

| 文档 | 完整度 | 评级 |
|---|---|:---:|
| `PROJECT_STATE.md` | 实时更新 | A |
| `ENGINEERING_LOG.md` | 37KB，按工作单元追加 | A |
| `DECISIONS.md` | 9KB，决策溯源 | A |
| `KNOWN_ISSUES.md` | 完整但部分过期（如 P0-2 与代码软降级矛盾） | B |
| `CONTINUITY.md` | 22KB，但部分口径与代码冲突 | B |
| `ARCHITECTURE.md` | 极简（1007 字节），不够详细 | C |
| `doc/DELIVERABLE_API_SPEC.md` | 31KB，但与代码字段不一致 | B |
| `doc/XRAY-全局图谱.md` | 20KB，部分发现已过期（如 R1 缺 fs/path 已修复） | B |
| `doc/minimal-runtime-loop.md` | 5KB，运行主链索引 | A |
| `.codebuddy/memory/MEMORY.md` | 9.9KB，长期口径 | A |
| `.codebuddy/memory/CONTEXT-DIGEST.md` | 11KB，上下文摘要 | A |

### 6.5 可维护性评级：**D（不合格）**

> 8 个超限文件 + 0 测试 + 11+ 空 catch + 4 处死代码 + 多处重复实现。建议在阶段二治理时优先拆分 AgentBDirect.vue。

---

## 七、兼容性评估

### 7.1 浏览器兼容性

| 浏览器 | 状态 | 评级 |
|---|---|:---:|
| Chrome 100+ | ✅ 完整支持 | A |
| Edge 100+ | ✅ 完整支持 | A |
| Firefox 100+ | ✅ 大部分支持 | B |
| Safari 15+ | ✅ 大部分支持 | B |
| IE 11 | ❌ 不支持（ESM） | D |
| 移动端浏览器 | ⚠️ 桌面优先 UI | C |

### 7.2 部署环境兼容性

| 环境 | 状态 | 评级 |
|---|---|:---:|
| Vite Dev (3001/3200/5199) | ✅ 完整支持 | A |
| Vite Preview | ✅ 完整支持 | A |
| Production Server (bun run start) | ✅ 完整支持 | A |
| Vercel Functions | ⚠️ 22 端点仅 6 个有 Function | D |
| Cloudflare Workers | ❌ 不支持（Node API） | D |
| Docker | ✅ 兼容 | A |

### 7.3 Vercel Functions 覆盖率

| 端点类别 | 总数 | Vercel Function 数 | 覆盖率 |
|---|---:|---:|---:|
| Recognition | 1 | 1 | 100% |
| Agent B | 1 | 1 | 100% |
| Check Agent | 3 | 3 | 100% |
| Handoff | 3 | 0 | **0%** |
| Screenshot | 1 | 0 | **0%** |
| Deliverable | 6 | 0 | **0%** |
| Knowledge | 2 | 0 | **0%** |
| TTS | 4 | 0 | **0%** |
| Cleanup | 1 | 1 | 100% |
| Health/Mock | 2 | 0 | 0% |
| **合计** | **22** | **6** | **27%** |

### 7.4 跨平台端口冲突

| 端口 | 项目 | 状态 |
|---|---|---|
| 3000 | 另一个项目 `G:\vedio\991-main-optimized-20260913` | ⚠️ vite.config.js 默认值与此冲突 |
| 3001 | 本仓（用户期望） | — |
| 3200 | 本仓（多实例） | — |
| 5199 | 本仓（多实例） | — |

### 7.5 兼容性评级：**C（合格）**

- 浏览器兼容性良好
- Vercel 部署严重缺 Function（73% 端点会 404）
- vite.config.js port=3000 与另一项目冲突

---

## 八、核心库依赖版本矩阵

| 库 | 当前版本 | 最新稳定版 | 落后版本 | 评级 | 备注 |
|---|---|---|---|:---:|---|
| `vue` | ^3.5.39 | 3.5.x | 0 | A | — |
| `ant-design-vue` | ^4.2.6 | 4.2.x | 0 | A | — |
| `@ant-design/icons-vue` | ^7.0.1 | 7.x | 0 | A | — |
| `katex` | ^0.18.4 | 0.16.x | — | A | 当前用 0.18.x 较新 |
| `rough-notation` | ^0.5.1 | 0.5.1 | 0 | A | — |
| `roughjs` | ^4.6.6 | 4.6.x | 0 | A | — |
| `vite` | ^8.1.1 | 8.x | 0 | A | — |
| `@vitejs/plugin-vue` | ^6.0.7 | 6.x | 0 | A | — |
| `eslint` | ^10.8.1 | 10.x | 0 | A | — |
| `eslint-plugin-vue` | ^10.10.0 | 10.x | 0 | A | — |
| `@eslint/js` | ^10.0.1 | 10.x | 0 | A | — |
| `node` (运行时) | — | 20.x LTS / 22.x LTS | — | A | `bun run start` 兼容 |
| `bun` (生产入口) | — | 1.2.x | — | A | `bun run start` |

**所有依赖均为最新稳定版本，无版本落后。** ✅

---

## 九、综合评级与建议优先级

### 9.1 6 维度评级汇总

| 维度 | 评级 | 关键短板数 |
|---|:---:|---:|
| 1. 性能 | C | 6 |
| 2. 可用性 | C | 5 |
| 3. 依赖复杂度 | B | 4 |
| 4. 安全性 | D | 12 |
| 5. 可维护性 | D | 8 |
| 6. 兼容性 | C | 3 |
| **综合** | **C** | **38** |

### 9.2 P0 立即修复清单（24 项）

| 类别 | # | 问题 | 影响范围 |
|---|---|---|---|
| 安全 | S1 | Fish Audio 双 Key 硬编码 | 密钥泄露 |
| 安全 | S2-S6 | 5 处目录穿越漏洞 | 任意文件读写 |
| 安全 | S7 | UPSTREAM_HOSTS 默认空 | SSRF |
| 可用性 | U1 | handdraw-player.html 缺失 | 产物预览 404 |
| 可用性 | U2 | /api/knowledge/revert 不可达 | 还原功能失效 |
| 可用性 | U3 | pic 目录 content-type 错配 | 音频副本无法播放 |
| 维护 | M1 | AgentBDirect.vue 5330 行 | 不可维护 |
| 维护 | M2 | 全项目 0 测试 | 任何改动无证据 |
| 维护 | M3 | 11+ 空 catch | 长期静默失效 |
| 兼容 | C1 | Vercel Functions 覆盖率 27% | 部署后 16 端点 404 |
| 兼容 | C2 | vite.config.js port=3000 冲突 | dev 启动失败 |
| 性能 | P1 | AgentBDirect 首屏解析慢 | 用户体验 |
| 性能 | P2 | B 缓存键缺 canvasParams | 用户改参数不生效 |

### 9.3 治理优先级建议

| 优先级 | 项数 | 建议阶段 |
|---|---:|---|
| 🔴 P0 立即修复 | 13 项 | 阶段二·遗留治理 |
| 🟡 P1 高优治理 | 9 项 | 阶段三·骨架施工 |
| 🟢 P2 中优治理 | 11 项 | 阶段四·API 通链 |
| ⚪ P3 长期治理 | 5 项 | 阶段五·UI 优化 |

---

> 性能评估表阶段一产出完毕。下一份《需求校准表》将对照文档与代码差异，识别隐藏逻辑与未实现功能。
