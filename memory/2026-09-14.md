# 2026-09-14 工作足迹

## 上午：板书子系统代码深度分析（阶段一）
- 产出 `doc/board_resource_list.md`、`doc/board_risk_matrix.md`、`doc/board_initial_state.md` 三份交付物。
- 在 `PROJECT_STATE.md` 追加变更树。
- 关键真实发现：
  - `rough-notation` 仅 question 区可用——`BoardContentLayer.vue` 只对 question 打 `data-board-region`，analysis/solution/summary 无 DOM 锚点，会抛「找不到标记区域」。
  - `draw` 意图工具执行路径空壳（`boardToolCatalog.js` 返回 execute no-op），真实几何由 rough-drawing 承担。
  - L4 用户画笔层仅空 div 占位。
- 运行代码零改动，仅分析文档与变更树留痕。

## 中午：战士排队问题口播板书讲学 HTML
- 读取 `若干名战士排成8列的长方形队列...-分镜表 (4).md` 与 `deliverable-deliverable-1789343131923.json`。
- 使用 `K:\5\public` 本地素材（canvas 边框、四区角标、pingfang-qiaomu 手写体）。
- 生成分布式资源与 CDN 库混合的讲学页 `K:\5\lecture.html`：
  - 内嵌 deliverable JSON 数据；
  - 按 `exclusiveExecutionPlan` 时间轴同步口播（Web Speech API）与板书动画；
  - 使用 rough.js / rough-notation 绘制辅助线、箭头、下划线；
  - 支持播放/暂停/重来/进度条点击；
  - 画布按 1726×980 比例自适应缩放。

## 下午：lecture.html 参数修正（按夏夏规范）
- 素材改用 ibb.co 外链替换 `K:\5\public`（canvas/question/analysis/solution/summary 五张）。
- 画布 1726×980 比例不变 + 统一 transform 缩放锁；html 基准 `font-size:30px`（1rem=30px），题目区微软雅黑 `1rem`，其他区 `1.35rem`（1.2~1.5x），均用 rem 不硬编码。
- 重写一维时间轴模型：每个 row = 原子单元（stage/audioUrl/speech/board/action 五槽位）；行内板书行与动作时间互斥、非重叠、逐行顺序出现（人写字节奏）；口播整行时长；row 间 1s 空白停顿；audioUrl 缺失时按 160字/分、板书 1~1.5s/3字 均值测算。实测总时长≈294s（与原 estimatedDurationMs 合计 289s 吻合）。
- 逐行书写 `writeBoardLine`：每行写完再写下一行，行内逐字带轻微抖动显现。
- 越界坐标（如解答末行 y:111.5%）钳制到 ≤90% 防裁切。
- ⚠️ 工具坑：同消息多 Edit 改同一文件会静默漏改（约每两个漏一个），必须每改完一处就 grep 校验再继续。已用此法补齐全部遗漏编辑。

## 傍晚：lecture.html 二次修正（按夏夏 12 条 handoff 渲染硬规矩）
- 夏夏给了 12 条规矩（handoff=真相源 / coordinateMode 决定坐标 / board 是 v2.0 对象 / B 不输出时间下游算 / 字号1.2~1.5x+抖动 / 偏慢+不够手写 / \frac念分母分之分子 / x念艾克斯 / 动作仅3种且整画布锚定文字 / triggerAt +00:00:SS / 达芬奇式弹性不溢出 / 松门槛）。逐条比对真实数据后落地：
  - 数据证实 `board` 已是 v2.0 对象、`coordinateMode` 字段缺失（按 percentage）、`triggerAt` 字段缺失（按顺序排、预留分支）、口播已写「艾克斯」——这 3 条已满足或已预留。
  - 新增 LaTeX 渲染：`renderLine()` 支持 `\frac`/上下标/`\begin{aligned}`/`\\`/`&` 轻量子集；行长按 `boardLines()` 切分（去 aligned、把 `\\` 当换行、`&` 丢弃）。
  - 手写感增强：`writeBoardLine` 整行 ±1.5° 倾斜 + 行距 ±10% 抖动 + 逐字位移/旋转抖动；板书速度 1.25→0.9s/3字。
  - 越界坐标钳入 [3,88]%（达芬奇弹性），不再只钳 90%。
  - 时间轴：整 row 时长 = 口播自然时长，板书/动作压缩进 speech（scale=1 验证，总时长≈294s 与数据 298s 吻合）。
- 验证：node --check 语法通过；真实数据跑 renderLine 无异常、单元数=char 数；时间轴 scale 全 1.00。
- 另存技能 `board-lecture-player`（~/.workbuddy/skills/），固化 12 规矩 + 数据 schema + 渲染模式，下次直接调。

## 晚上：lecture.html 补「门卫转义层」+ 字体/运算符/分数规范（5.1/5.2/5.3）
- 夏夏补充 3 条硬规矩（手写字体与数学排版规范）：门卫转义层、5.1 字体统一手写体不切换、5.2 乘号→小写 x 禁 ×、5.3 分数用 \frac 上下结构禁 7/15。
- 真实数据核对：数据真有 `\times`（row6 `60\times4`）、`^2` 上标、`\Rightarrow`；无 `\div`、无字面 ×÷、无斜杠分数。
- 新增 `sanitizeBoard()` 门卫层（插在 boardLines 后、renderLine 前）：`\times`→`x`；`\div`→`\frac`；删字面 ×÷；`\d+/\d+`→`\frac`（防御 5.3）。
- renderLine 的 cmds 删 `\times`/`\div` 印刷映射，杜绝 ×÷ 漏出。
- CSS 锁死 5.1：`.frac`/`.sup`/`.sub` 只缩放字号、不设 font-family（继承手写体）；题目区微软雅黑是夏夏上条指定的用户预设，合规。
- 验证：node --check 通过；真实数据跑 sanitizeBoard+renderLine，`240=60\times4`→`240=60x4`（手写体 x，零 ×÷ 残留）；CSS 分数/上下标无字体切换。
- 待夏夏拍板：5.3 分数当前用轻量手写体 \frac 渲染（视觉=上下结构，满足 5.1 不切字体），未引 KaTeX 引擎；若要真 KaTeX 库可换但牺牲逐字手写感且需处理字体覆盖。

## 晚更：上标缩小 + 符号常见凑 + 自然换行排版
- 夏夏补充细节：数字平方=缩小字在上（上标）；手写体符号尽量用常见凑，字体不支持就自然段落换行；页面排版注意。
- 上标已满足：`.sup { font-size:.66em; vertical-align:super }`，renderLine 的 `^`/`_` 生成 `<span class="char sup/sub">`；真实数据 `a^2-b^2` 验证 `2` 正确缩在上。
- 排版修复：`.board-content`/`.board-line` 由 `white-space:pre` 改 `pre-wrap + overflow-wrap:anywhere`，板书超长按自然段落折行不溢出；同行多行 block flow + marginBottom 堆叠不重叠，跨 row 各自绝对定位不互相压。
- 符号"常见凑"修复：cmds 映射原全为生僻 Unicode（⇒⇐✓≈≠≤≥·±）手写体必缺字变豆腐块，全部换 ASCII（`\Rightarrow`/`\to`→`->`、`\impliedby`→`<-`、`\pm`→`+-`、`\cdot`→`*`、`\approx`→`~`、`\neq`→`!=`、`\leq`→`<=`、`\geq`→`>=`、`\checkmark`→`V`）；根号 `√` 在 5.1 白名单保留。
- 验证：node --check 通过；真实数据 `2a=64 \Rightarrow a=32`→渲染 `->` 且零 `⇒`；全量扫描含 `\Rightarrow`/`\times` 行均正确（`\times`→x、零生僻 Unicode 残留）。
- 待拍板：`\Rightarrow` 用 `->` 还是手写体更可能支持的 `→`（U+2192，仍可能缺字）；当前选 `->` 绝对安全。

## 画布比例锁死 + 缩放锁定（夏夏硬约束）
- 约束：长宽比绝对不可变（1726×980）；缩放默认锁定，变化须点击「解锁缩放」。
- 数据真相：`deliverable.boardPlan.canvas={w:1726,h:980}` 是唯一真相源；`meta.canvasSize` 是重复字段。原 HTML 硬编码 1726/980 未读数据、且 fitBoard 在 deliverable 定义前调用（TDZ 风险）。
- 改造：删除旧硬编码 fitBoard 块；在 `const rows` 之后读 `deliverable.boardPlan.canvas` 并断言 `w===1726 && h===980`（不符则 errorTip + return 拒绝渲染）；`board.style.width/height` 从数据设，覆盖 CSS 硬编码，单一真相源。
- 比例锁死：`applyScale(s)` 用单一 scale 值同时作用于 transform:scale(x,y)，宽高同比，比例恒不变。
- 缩放锁定：`scaleLocked=true` 默认；锁定态下 `window.resize` 不重 fit、滚轮禁用；解锁态 resize 重 fit + 滚轮等比 zoom（0.3~3）。控制栏新增 `🔒 缩放已锁定（点击解锁）` 按钮切换。
- 验证：node --check 通过；grep 确认旧 `availW/1726` 已删、新 canvas 断言/按钮/scaleLocked 就位。
