# 2026-09-14

## 修复：代码页 ASR 优化按钮消失
- 现象：`agent-b-v2.html`（AgentBDirect.vue）五字段执行表 #extra 区看不到 ASR 按钮。
- 根因：`<div v-if="false" class="action-btn-group group-asr">` 写死隐藏，组内 `ASR 兜底转换`（runInstantMathAsrPolish）、`Check Agent`、`还原` 三按钮均不渲染。逻辑与 CSS（`.btn-asr-fallback`）仍完整。
- 改动：仅删除该容器的 `v-if="false"`（1 行）。其余隐藏项 `group-deliverable`、api-spec 按钮、api-spec menu-item 仍然保持 `v-if="false"`，未触碰。
- 验证：eslint 0 错；`@vue/compiler-sfc` parse/compileTemplate 0 错；dev server `http://127.0.0.1:3000/agent-b-v2.html` 200、`/src/agent-b-v2/AgentBDirect.vue` 200 且含 `group-asr`。
- 提示：`vite build` 因缺 `lite-player.html` 入口而失败，属既有问题，与本改动无关。

## 排查：页面素材图片加载失败（未改码）
- 结论：不是 public 素材丢失。静态图片引用全部命中 public 且文件存在：`/canvas.png`（RealBoardPreview 默认 canvasImageUrl）、`/topic.png`、`/analysis.png`、`/solution.png`、`/summary.png`（BoardContentLayer）、`/favicon.svg`、`/fonts/pingfang-qiaomu.ttf`。
- 真正根因：`src/components/Step1Entry.vue` 两个预览 modal 的 `<img>` 未判空——1229 行 `:src="screenshotUrl"`、1242 行 `:src="sourceImageUrl"`。空串 src 被浏览器解析为当前页 URL（`http://127.0.0.1:3000/agent-b-v2.html`），控制台报 `Failed to load IMG`。
- 触发面：`screenshotPreviewOpen`/`sourceImagePreviewOpen` 仅由点击已渲染缩略图打开，正常路径不会空 src；属防御性缺陷。
- 次要：`src/index.css:1` 外链 Google Fonts（`fonts.googleapis.com`），离线/无网时会失败，属外链依赖非 public 素材。
- 待用户确认后再改；最小修复=两个 img 加 `v-if` 守卫。

## 修复：demo.html 图片改用 public 本地素材（三方图床 i.ibb.co 丢失真因）
- 现象：`skills/demo.html` 四个区域角标（题目/分析/解答/总结）与画布背景图不显示。
- 根因：素材指向三方图床 `i.ibb.co`（ImgBB）：4 个 `<img src="https://i.ibb.co/.../*.png">` + `#cfg-bg` 默认值 `https://i.ibb.co/pvBCJjSG/canvas.png`。三方图床不可靠（限流/失效/被墙）即表现为"素材丢失"；本地 `public/` 早已有同名文件。
- 改动（仅 `skills/demo.html`，5 处）：
  1. L120-123 角标 → `/topic.png`、`/analysis.png`、`/solution.png`、`/summary.png`
  2. L66 CSS `url(canvas.png)` → `url(/canvas.png)`（原相对路径会解析成 `/skills/canvas.png` 404）
  3. L159 `#cfg-bg` value → `/canvas.png`
  4. L582 `getConfig()` 兜底 `bg` → `/canvas.png`
- 验证：`rg i\.ibb\.co` 在 skills/、src/ 均 0 命中；磁盘文件 `skills/demo.html` 76799B、`/canvas.png` 3 处、`i.ibb.co` 0 处。
- ⚠️ 踩坑：端口 3000 的 node/vite 进程属**另一个项目** `G:\vedio\991-main-optimized-20260913`（pid 13480，CommandLine 可见），不是本工作区。所有「用 http://127.0.0.1:3000 验证本工作区页面」的结论都不可信，包括本文件上方 ASR 修复那条 200 验证。本工作区验证必须自起 vite（换端口如 3200）。
- 真实验证（本工作区自起 vite，`node node_modules/vite/bin/vite.js --port 3200`，pid 44480）：`/topic.png` 200/7048B、`/analysis.png` 200/8051B、`/solution.png` 200/8692B、`/summary.png` 200/8117B、`/canvas.png` 200/60189B、`/skills/demo.html` 200/76763B；页面内 `i.ibb.co` 残留 0、本地 `<img src="/*.png">` 4 处、`/canvas.png` 3 处。
- 约定：项目图片素材一律引用 `public/` 下的根绝对路径（`/xxx.png`），禁止外链三方图床。

## 盘点：public/ 画布素材与引用对照
- 素材（尺寸/内容）：`canvas.png` 1726×980 蓝色圆角画布框（L1 底图，CONTINUITY.md L128）；`topic.png` 97×39 题目角标；`solution.png` 101×43 解答角标；`analysis.png` 99×41 分析角标；`summary.png` 99×40 总结角标。
- 引用：`RealBoardPreview.vue:14` → `/canvas.png`；`BoardContentLayer.vue:516/636/639/642` → topic/analysis/solution/summary；`skills/demo.html` 4 角标 + `#cfg-bg` + CSS，均同一套。
- **发现重复真相**：`public/question.png` 与 `public/topic.png` 字节完全相同（MD5 `EFB659011C60D24197E261D369358377`，均 97×39 / 7048B），且 `question.png` 全项目零代码引用（grep 只命中 "question" 作为区域名 question/analysis/solution/summary）。属冗余副本，待用户决定删除或改引用。

## 结论：solution 素材命名无需改动 + demo.html 修 1 个真实崩溃
- 用户曾要求「重命名素材成 solution」，二次确认后答复「无需改名，已是 solution.png」。即 `public/solution.png` 就是解答角标，命名本就正确，不动文件。
- 修复 `skills/demo.html:702`：`rows.forEach(row => {` 缺 index 参数，但同体内 720 行 `rowEl.dataset.rowIndex = idx` 引用了 `idx` → `ReferenceError: idx is not defined`，导致 `applyConfig()` 在第 698-745 的 board 渲染循环中整体中断，其后的画布尺寸/背景图/角标定位（L623-632）全部不执行。改为 `rows.forEach((row, idx) => {`。注意 L659 的 `rows.forEach(row => {` 体内不用 idx，无需改；L768 本就有 idx。
- 验证：`node -e "new Function(<inline script>)"` 解析 OK；`Select-String rows.forEach` 确认 L702 已带 idx。未能做浏览器级验证——`browser-use` CLI 在本机未安装（doctor 报「术语 'browser-use' 不被识别」）。
- 注：IDE WebView 报出的 `[applyConfig] ReferenceError: idx is not defined` / `[fullReset] TypeError` / `getBoundingClientRect` 三条，URL 都是 `http://127.0.0.1:3000/skills/demo.html`（另一个项目）；本工作区 demo.html 的 `$progBar/$progTxt/$tts/$sync` 绑定的 id 均真实存在（progBar/progText/ttsLive/syncStat），故后两条不适用于本文件。

## 首页真相核查：无登录功能 + 两份副本四端口并存（乱源）
- 首页渲染链（已按行核实，无路由库）：`index.html` → `/src/main.js` → `src/App.vue`（仅 11 行，渲染 `<DirectFlow />` + `<GlobalPulseLoading />`）→ `src/agent-b-v2/DirectFlow.vue`（`page` 初值 `'step1'`）→ 首屏 **`src/components/Step1Entry.vue`**（顶栏"教学板书"+标签"第1步 · 贴题识别"，document.title「画布 · 第1步 贴题识别」）→ 点确认触发 `enterAgentB()` → `fetch('/api/handoff')` → 渲染 **`src/agent-b-v2/AgentBDirect.vue`**（title「生成 · Agent B 五字段」）。
- **全项目没有登录/账号/鉴权代码**。全仓 grep `登录|登出|logout|signIn` 仅命中 `src/lib/userApiConfig.js:14` 注释「退出登录/换号」（语义=清空 API Key）。首页右侧"设置"抽屉与 `userApiConfig`/`checkAgentApiConfig`/`agentBApiConfig` 三套 API 配置才是像"登录"的东西。
- 端口-项目对照（2026-09-14 实测监听）：3000→pid 13480 = `G:\vedio\991-main-optimized-20260913`（**非 git 仓库**，目录 LastWriteTime 09-14 02:42）；3001→pid 16100 = 本工作区 dev（`npm run dev` 固定 `--port 3000` 抢不到，vite 自动 +1）；3200→pid 44480、5199→pid 52308 也都是本工作区 vite。**四个端口首页 title 全相同**，肉眼不可区分，极易误判。
- 两份差异（`git diff --no-index --stat` 991 vs 本工作区 `src/`）：23 文件 +1381/−368；大头 `BoardContentLayer.vue` +831、`AgentBDirect.vue` +338、`Step1Entry.vue` 87 行差。`index.html`/`src/main.js`/`src/App.vue` 三文件 MD5 **SAME**（同一外壳），`DirectFlow.vue`/`AgentBDirect.vue`/`Step1Entry.vue`/`userApiConfig.js` **DIFF**。
- `DirectFlow.vue` 差异语义：991 那份 `enterAgentB(sessionHandoff)` 支持会话内直接带 payload 进 B（注释「生产无持久化文件系统时不阻断 Demo」）；本工作区改为**只从 `/api/handoff` 实体文件读**。
- 端到端验证（编译产物级）：请求 `/src/components/Step1Entry.vue` 3000=326563B vs 3001=339740B、`/src/agent-b-v2/AgentBDirect.vue` 3000=829162B vs 3001=871928B，均 DIFF；`/api/handoff` 3000=7145B vs 3001=4798B 也不同 → 两端口确为两套代码两套存档。
- 本工作区（3001）当前 handoff 挂载题目：「若若干名战士排成8列的长方形队列，若增加120人或减少120人都能组成一个新的正方形纵列。那么，原有战士（ ）人。」，`confirmedAt` 2026-09-14T07:36:48.814Z。

## 「夏夏说的话」全仓索引（grep「夏夏」，13 文件 22 处）
- **仅 2 处是带引号原话**，都引自 `truth/territory-C-actions-and-anchors.md`：L26「动作 triggerAt 是本 row 开始后多少秒…建议以口播稿某个关键词念到为锚点」；L41「板书坐标推荐 region 区域内…写不下时往旁边写，达芬奇手稿感风格，弹性调整，不要让内容溢出画布」。
- 其余均为转述/署名：题型分类库待整理（`problemTypeClassifier.js:5`、`Step1Entry.vue:335-336`）、表稿尺寸 1892×1044（`canvasCoords.js:4,18`）、图像硬边界「纯文字题不贴原图」（`Step1Entry.vue:439`）、课堂话术不得机械拼接（`speechTiming.js:158`）、下划线+画圈为指定兜底动作（truth L10）、交付物要与 JSON 配对的 HTML 归档（`deliverableStoreHandler.js:179`）、Knowledge-A 由夏夏提供（`PROJECT_STATE.md:16`）、UI「夏夏专属」（`style.css:19,228`、`index.css:18`、`AgentBDirect.vue:316,422,2149,3790,5223`）。
- **阻塞项**：`doc/board_risk_matrix.md:8` 表头「建议（待夏夏确认后执行）」+ `PROJECT_STATE.md:212`「R2/R4/R5 待夏夏确认后单独清理」。
- **非发言**：`doc/建议提示.md:2`「你是夏夏老师的 AI 助教 小B」（角色设定）、`public/README.md:1`「夏夏版」、`PROJECT_STATE.md:151-152` Vercel 账号 `xiaxia`。
- 别名核查：「甲方」13 处均指真画布（`canvasCoords.js:3`「甲方真画布 1726×980」），非夏夏发言；无「夏老师/XiaXia 说」。
- 未落地（用户中途改话题）：`.gitignore` 排除 public 运行产物方案。

## 需求：板书手写字体必须走 zeoseven 网络字体（handoff canvasParams 补 fontSource）

## 树干主干与领地蓝图：新建 truth 唯一真相基准文件夹
- 目标：平息多 Agent 各自为政乱丢垃圾的问题，确立 0 置信原则，自主干向树枝延展，划分专有领地。
- 产出文件夹：`truth/`
  - `truth/00-TRUTH-BASE.md`：全局树干主干地图、六大领地（A: Agent B, B: Contract, C: Tools, D: Speech, E: Aesthetics & Render, F: Check Agent）及蛀虫排查警戒线。
  - `truth/territory-A-agent-b.md`：Agent B 核心契约与防违规红线。
  - `truth/territory-B-contract.md`：合同守门人与兜底防溢出红线。
  - `truth/territory-C-tools.md`：四工具白名单与单手串行红线。
  - `truth/territory-E-render.md`：手稿美学（微斜、随机抖动）归属渲染层红线。
- 原则锁定：渲染算法绝不塞给大模型；单手串行单向推进；废弃旧版 triggerAt 格式；一切以 handoff 为真实输入。
- 领地 C 动作核心事实固化：
  - 动作 2 个核心兜底：下划线 (`underline`) 与 画圈 (`circle`)，支持全画布 4 个区域（question/analysis/solution/summary）。
  - 核心要求：必须通过 `target.exactText` 锚定真实文字，依赖浏览器 Range 定位，严禁虚空标记否则悬空漂移。
  - 起笔时机：口播念到关键词为真实语义锚点，模型根据语境判断时机，转换为起手延时（代码层兼容秒级延时与 +00:00:SS 格式）。
  - 达芬奇手稿感与弹性区域：允许在区域内像草稿般弹性伸缩、写不下时顺延留白，代码层 `sanitizeRowLayout` 兜底限位防溢出。
  - 产出落地：写入 `truth/territory-C-actions-and-anchors.md`。
- 用户要求：分析/解答/总结的字体使用 `https://fontsapi.zeoseven.com/490/main/result.css`（`font-family: "LikeJianJianTi"; font-weight: normal`），用 preload+onload 切 stylesheet 的方式加载，noscript 兜底。
- 改动（2 文件，同一套内容）：`src/services/stepHandoff.js:147-166`（真实生成源，handoff 里 canvasParams 由它产出）与 `server/handoffStoreHandler.js:47-...`（缺失时的兜底补全）。两处同构，避免第二套真相；只改这里，`ensureHandoffCompleteness` 的兜底在已有 canvasParams 时不会生效，所以必须同时改 stepHandoff.js。
- 新增 `canvasParams.fontSource.handwriting`：`family=LikeJianJianTi`、`fontWeight=normal`、`cssHref`、`loadSnippet`(preload+onload)、`fallbackSnippet`(noscript)、`usage` 说明（不加载就回退系统字体）；`fontSource.question` 标注印刷体走本地不加载网络字体。fontSize 三行 family 后缀加「网络字体 fontSource.handwriting，font-weight: normal」。
- 验证：eslint 两文件 0 错。未做端到端渲染验证（需重新生成 handoff 才会在新文件里出现该字段；旧 handoff 文件不含 fontSource）。
- 关联事实：`src/board-tools/boardTypography.js` 里 `like-jianjianti` 已是同一 href（`.../490/main/result.css`），默认却是 `DEFAULT_HANDWRITING_FONT_ID='qiaomu-local'`；`skills/demo.html` 的 `cfg.boardFont` 默认 `LikeJianJianTi`。若要默认改成 490 字体，应改 boardTypography 默认 id（本次未动，超出用户指定范围）。

## 盘点：手写字体引用全景（排除 public 产物）
- 唯一候选真相：`src/board-tools/boardTypography.js`（5 款，490=LikeJianJianTi）+ `boardTypography.css`（本地 @font-face `Qinghuabu Qiaomu` ← `/fonts/pingfang-qiaomu.ttf`）。
- **该模块在 src 内无任何功能调用**：只有 `src/main.js:5`、`src/board-preview/main.js:5` import 了它的 CSS；`getHandwritingStyle/ensureHandwritingFont/loadBoardTypographyConfig` 全仓零调用（CONTINUITY.md:216 已记「冻结等独立播放器」）。→ src 应用内目前没有任何落点真正应用手写字体。
- 真实落点在 `skills/demo.html`：`link` 直连 157/490/511/510（rel=stylesheet，非 preload 方式）+ `:14 @import .../509/...`，`:647` 把 `cfg.boardFont` 写到 region 容器；但 `:72-87` 面板文本硬编码 `'PingFangSaTuoTi'`，不跟随下拉，且 509 不在 boardTypography 列表 → 第二套字体真相。
- 三个根 HTML（`index.html`/`agent-b-v2.html`/`board-preview.html`）**0 处 font 引用**，无 zeoseven link。
- `server/renderDeliverableHtml.js` 全文 0 处 font，且依赖 `public/deliverable/handdraw-player.html`（**该文件不存在**，目录只有 current.html/json + SPEC + schema）→ 交付页恒走降级重定向。
- 数值分裂：`stepHandoff.js:180` 行高公式 38px vs `handoffStoreHandler.js:83` 32px；`CONTINUITY.md:332-334/342` 仍 32px；`doc/DELIVERABLE_API_SPEC.md:261` PPTX 映射写 `fontFace: 'KaiTi'`（与 LikeJianJianTi 不符）。
- 待确认拼写：`boardTypography.js:28` `PING FANG SHAGN SHANG QIAN`（SHAGN 疑为字体商原名，不能擅自改）。

## 审计复核（100% 怀疑式）+ 修复执行（2026-09-14）
- 复核结论：上条盘点整体达标，两处修正——① `skills/demo.html:14` 的 509 就是 `PingFangSaTuoTi`（已 fetch 证实），所以「硬编码字体」不是未加载，而是**默认 `cfg.boardFont=LikeJianJianTi` 却只加载了 509** 的错配；② 影响升级：`src/agent-b-v2/AgentBDirect.vue:1427` 打开 `/deliverable/handdraw-player.html`，该文件**全仓不存在**（`*handdraw*.html` 0 命中）→ 主界面「🎬 教学微课演播」按钮必 404。
- 已改（5 文件）：
  1. `src/board-tools/boardTypography.js:2` `DEFAULT_HANDWRITING_FONT_ID` 'qiaomu-local' → 'like-jianjianti'（唯一默认真相）。
  2. `index.html` / `agent-b-v2.html` / `board-preview.html` head 各加 490 的 preload+onload + noscript（只加载字体，不改 body 字体）。
  3. `skills/demo.html`：删除 10 处硬编码 `'PingFangSaTuoTi',`；`:14` @import 509 → 490（与默认 cfg.boardFont 对齐）；applyConfig 内新增给 `.board-panel` 设 `cfg.boardFont`。
  4. `server/handoffStoreHandler.js:83` 行高示例 32px → 38px（与 stepHandoff.js:180 一致）。
- 用户新规：**代码不得使用境外 Google 字体**（加载不出来）。已删 `src/index.css:1` 的 `fonts.googleapis.com` @import，`--font-body/--font-headline` 改本地系统栈（PingFang SC/Microsoft YaHei/微软雅黑/system-ui）。全仓仅剩 `public/snapdom.js`（第三方库，非引用）。
- 验证：eslint 0 错；490 CSS 可访问且 `font-family:"LikeJianJianTi"`；临时脚本已清理。**`vite build` 仍失败，但属预存在问题**：`vite.config.js:46` 入口 `lite-player.html` 不存在，与本次改动无关（同类缺失还有 handdraw-player.html）。
- 未动（需用户决策）：`CONTINUITY.md` 32px 旧值、`DELIVERABLE_API_SPEC.md:261` `fontFace:'KaiTi'`、缺失的两个播放器 HTML 模板。

## 境外资源全仓复查 + rough.js 本地化（用户追问「其他地方是否还有同样类型错误」）
- 扫描口径：全仓（排除 public/、node_modules）搜 `googleapis|gstatic|jsdelivr|unpkg|cdnjs|cloudflare|ibb.co|githubusercontent|fontawesome|jquery|bootstrapcdn` 及 `(src|href)="https://`、`url(https://`、`from 'https`/`import('https`。
- **唯一真实境外 JS 依赖**：`skills/demo.html:12` `cdn.jsdelivr.net/npm/roughjs@4.6.6` —— 失败时 `rough` 未定义 → `rough.canvas(cv)`（:878）抛错 → 整页画布/配置逻辑崩（比字体严重，字体失败只回退系统字体）。
- 已修：复制 `node_modules/roughjs/bundled/rough.js`(27762B) → `public/rough.js`，`:12` 改 `<script src="/rough.js">`（与 `public/snapdom.js` 本地化同例，snapdom 早已从 unpkg 改为 `/snapdom.js`，`index.html:43`）。
- 保留且合理（非错误）：`fontsapi.zeoseven.com` 157/490/511/510 四处 —— 用户指定的板书字体源，且 demo.html `:182-187` 有字体下拉需按需切换；`public/katex/`、`public/fonts/SKILL.md` 里的 jsdelivr/unpkg 只是第三方 README 与技能文档文字，非引用；`newapi.prorisehub.com` 是业务 API 端点。
- 验证：本工作区 dev（3001/3200/5199 三个实例）`/rough.js` 均 200/27762B；`/skills/demo.html` 中 `jsdelivr:False`、`src="/rough.js":True`。注意 3000 是**另一个项目**（返回 2119B 非本仓文件），勿用它验证。

## 行高口径全局对齐（用户纠正记忆：自然换行 + 微微随机抖动）
- 用户定真相：**不预置固定行高数值，按当前字号自然换行，渲染层加微小随机抖动营造人手写感**。旧「32px/38px × 1.7 ÷ 980」公式作废。
- 已改（4 文件，声明层全部去掉写死数值）：
  - `src/services/stepHandoff.js` `lineHeight.others` + `lineHeightFormula`
  - `server/handoffStoreHandler.js` 同两处（与上一处字面一致，消除第二套真相）
  - `skills/board-speech-rules/SKILL.md` 1.3 表三行 + 「行高估算公式」代码块 → 改为行高规则说明
  - `CONTINUITY.md` 9.2 表：其他区行高改「自然换行」，删除 32px 公式与 5.55% 示例；顺带把分析/解答/总结字号 32px 修正为真实值 **38px**（与 stepHandoff/handler/prompt.js:36/agentBV2Handler:87 一致）
- 未改（有意）：`skills/board-speech-rules/meta.json:42` 是 changelog 历史字符串；`public/handoff/*.json` 是历史产物快照，重新生成即生效；`prompt.js:60`「按平均值估算行高即可，不需要考虑抖动」本就与新口径一致；demo.html 伪随机实现（1.65~1.95）即「微微随机抖动」的落地。
- 长期口径已写入 `MEMORY.md`。
- 遗留：`public/handoff/current.json` 等产物仍带旧「约1.7（1.55-1.85）」口径，下次生成覆盖。

## 画布默认字号对齐（用户定真相：板书=题目1.2~1.5倍，推荐约35px）
- 用户口径：画布 1726×980；题目 30px 微软雅黑；分析/解答/总结字号 = 题目的 1.2~1.5 倍，**约 35px**；字体用修正后的 LikeJianJianTi。百分比坐标规则不变，举例不改规则。
- 已改（7 处，38px→35px + 倍率表述）：
  - `src/services/stepHandoff.js` `fontSize`（三区 px 35；question 文案统一为「微软雅黑（印刷体，30px）」）
  - `server/handoffStoreHandler.js` 同上（与上一处字面一致）
  - `src/agent-b-v2/prompt.js:36`、`server/agentBV2Handler.js:87`、`:167` 三处 prompt 文案
  - `skills/board-speech-rules/SKILL.md` 1.3 表三行、`CONTINUITY.md` 9.2 表三行
  - `src/agent-b-v2/contract.js:201` 注释字高 38px→35px（2.5%→2.3% 画布宽）
- 有意未改：`skills/board-speech-rules/meta.json:41` 是 changelog 历史字符串（「32px调整为38px」留痕）；`public/handoff/*.json` 历史产物。
- 验证：lint 0 报错；全仓搜 `38px` 源码/文档已清零（仅剩 changelog 与历史产物）。

## 零置信审查（用户要求假设结论全错重新取证）
- 逐项取证结果，此前结论全部成立：① 实际服务的 `handoff-20260914-171204-505.json` mtime 17:12:04 < 真源修改 17:35:33 → 38px 确系旧存档；② 生成链路 `Step1Entry.vue:9` import `buildStep1Handoff`（同真源文件），node 实测 buildCanvasParams 输出 A/S/SUM=35px → 重新生成即 35px；③ 全仓正则扫字面量 `canvasParams: {` 仅剩 `AgentBDirect.vue:1139`（展开真源常量 ref，非字面量）；④ server/ 扫行高/1.65/1.8/1.5 零命中；⑤ 依赖单向（stepHandoff 零依赖被 9 文件引用），node 全链加载无环。
- 教训：验证 `current.json` 时曾把整个 JSON 内容误拼成文件名（我的取证失误），从指针 JSON 里取 `filename` 字段才是正确做法。

## 归一化全局真相（goal 模式执行完毕）
- 用户需求：清理歧义/争执/多处互相干扰；非技术背景，只认一套口径。
- 用户最终原话口径：画布 1726×980；题目 30号 微软雅黑；分析/解答/总结 = 题目字号 1.2~1.5 倍；LikeJianJianTi（490 CSS）；百分比坐标规则不变、计量单位全局一致。
- 实现（零新文件）：真源 = `src/services/stepHandoff.js` 顶部常量（CANVAS_SIZE/QUESTION_FONT_SIZE/QUESTION_LINE_HEIGHT/BOARD_FONT_SIZE/BOARD_FONT_RATIO_TEXT/LINE_HEIGHT_RULE/LINE_HEIGHT_FORMULA）+ `buildCanvasParams()`。
- 消费点全部改 import（9 文件）：`server/handoffStoreHandler.js`（删 37 行同构副本）、`server/agentBV2Handler.js`、`src/utils/canvasCoords.js`、`src/utils/boardLayout.js`、`src/lib/speechMarkdown.js`、`src/agent-b-v2/prompt.js`、`contract.js`、`AgentBDirect.vue`、`BoardContentLayer.vue`。
- 表现层保留（CSS 不能 import JS）：aspect-ratio、`.topic-text` 1.65（已加注释指向真源）、BoardPreviewApp 展示文字。
- 验证：lint 0 错；node import 链全通；vite 编译 Vue 均 200。
- `contract.js` minXGap 原 38px 改 `BOARD_FONT_SIZE+3`（语义=字高+余量，值不变）。

## 记忆纠错（用户：demo/html 测试页不是真相；字号行高多处控制）
- 用户指出口径混乱根因：**多个地方各自控制字号/行高**，且 demo 页把水搅浑。
- 已作废的旧记载（此前写进 MEMORY.md 的错误）：「渲染层实现真相 = `skills/demo.html:733-742` 伪随机 1.65~1.95」。用户明确：**`skills/demo.html` 及 skills 下所有 html 都是测试页，错误很多，不是真相**。其单位体系与正式代码不同（`html{font-size:10px}` + rem：题目 1.2rem、板书 row 1.4~1.6rem、行高 1.6/1.8/伪随机），不能拿来反推或校验正式口径。
- 同步作废：`public/handoff/*.json` 只是历史快照，非真相源。
- 行高/字号实际分散点（已逐行核实）：`stepHandoff.js:163-181`、`handoffStoreHandler.js:67-83`、`boardLayout.js:25/86/125/157/212-216`、`AgentBDirect.vue:250-257`、`BoardContentLayer.vue:83/105/682`、`prompt.js:36`、`agentBV2Handler.js:87/167`、`contract.js:201` + 两份文档。
- 排除（勿当乱源）：`BoardPreviewApp.vue` 的 line-height 是管理界面 CSS；`textTargetRegistry.js:92-93` 只是透传 computed style。
- 已写入 `MEMORY.md` 新章节「真相源分级」+「已知乱源：字号/行高多处硬编码」，收敛方案（抽常量模块）待用户拍板。

## 只读审计：meta.json 12 条口径（用户称是实测后新增的优化意向，**未落码，待裁决**）
- 证据结论（逐条）：✅已实现 #3（board v2.0 对象，`speechMarkdown.js:193-204` 兼容 v1）、#4（B 侧 contract.js `duration` 0 命中，时长由 `timing.js`/`speechTiming.js` 下游算）、#6（boardSpeed 1秒2字 ±10%）、#7（`\frac{}`+分母分之分子：`boardMathPolicy.js:8/44`、`mathAsrConverter.js:64`）、#9 主体（3 工具）、#12（松门槛：`SKILL.md:292-297`）。
- ⚠️冲突/待裁决：#1「handoff 唯一真相源」与我刚做的**真源常量**（`stepHandoff.js` 常量 + `boardLayout/speechMarkdown` import 兜底）并存——严格按 #1 则这些 import 应改为从传入 handoff 读值；#2 `coordinateMode` 在 `SKILL.md:31` 声明「不在 handoff 内、单独传」，但 `speechMarkdown.js:88` 从 canvasParams 读，而 `public/handoff/*.json` 无该字段（0 命中）→ 选 pixel 时展示仍恒显「百分比」，且新 `buildCanvasParams()` 也未产出该字段；#8 后半「行数→航数」**仅 meta.json 一处，代码未实现**；#9 `server/proxySelfCheck.js:272` 白名单仍含第 4 个 `'draw'`；#10 `triggerAt +00:00:SS` 只存在于文档（`SKILL.md:203/317`、`CONTINUITY.md:350`），contract 无解析；#11「可往旁边写」与 `prompt.js:47`/`contract.js:160-162,266` 的「区域 y+h 硬约束兜底限位」直接冲突，需定硬软。
- 另：#5 的 35px 与 1.2~1.5 倍（30×1.2=36）矛盾仍未定；#4 精确口径待澄清——`startDelay` 属时间字段但存在且被 Check 校准（`checkAgentHandler.js:145`）。
- 未改任何代码，未写入长期记忆，等用户确认。

## X-RAY 四层全局图谱（已产出 doc/XRAY-全局图谱.md，只读勘探）
- 分 4 域并行勘探：入口主链 / 渲染与 board-tools / 契约服务数据层 / server。结论：**全仓无循环依赖（DAG）**。
- 枢纽（入度）：`stepHandoff.js`≈10（唯一真源，零内部依赖）· `boardToolCatalog.js`5 · `canvasCoords.js`5 · `contract.js`4 · server 侧 `http.js`（全部 handler 依赖）。
- 完整产出在 `doc/XRAY-全局图谱.md`：全局分层依赖图、真源消费图、主业务流时序图、服务端路由全图、L4 函数签名+参数+副作用表、校验规则表、数据结构树、ontology 关系图、可复用清单、死代码清单、12 项风险矩阵。
- 高风险项（未修）：`checkAgentHandler.js:243` 用未 import 的 path/fs 被空 catch 吞；`knowledgeRefineHandler.js:88` `/revert` 分支不可达；`sanitizeRowLayout` 下界限位致多行粘连；`fishAudioHandler.js:12-15` 硬编码 Key；`vite build` 缺 lite-player.html 入口；`handdraw-player.html` 不存在。

## 2026-09-14：R1 四区文本锚点与动作传递修复
- `RealBoardPreview.vue` 现在从当前五字段 `rows` 在 L3 渲染 analysis/solution/summary 的真实文本锚点，`target.exactText` 可按区域查找；`startCoord` 兼容百分比和像素并复用 1726×980 真源换算。
- `BoardPreviewApp.vue` 汇总当前 `rows.actionSpec` 并传给预览层，未新增合同字段或第二份数据源。
- 验证：Vite 3200 对两个 Vue 入口及 `stepHandoff.js` 均返回 200，`git diff --check` 通过；未完成浏览器级四区 exactText 点击验收，后续不得标记完全收口。

## 任务1：Agent B 契约与提示词收口（板书不再生成起手坐标）
- 口径：board = content + startDelay 两字段；起手坐标/行高/行距/横向错位不进模型输出，排版归渲染层；coordinateMode 只约束 actionSpec 的 start/end。
- 改动：`src/agent-b-v2/prompt.js`（zoneAnchors 口径、截图条、删除解答区硬编码「X: 55%~92%/起手 Y%」残留、7.3 检查条）、`server/agentBV2Handler.js`（4 处口径 + 提醒标题改为「内容排版提醒」）、`contract.js`（修 no-useless-escape，语义等价）、`AgentBDirect.vue`（删 5 个坐标时代死 CSS，模板零引用）、`truth/` 三份文档同步。
- 证据：node 实测 normalizeBoard 剥离 startCoord；`public/board-result/board-result-20260914-215611-056.json` 全文无 startCoord；prompt.js 中 `startCoord`/`X: 55%~92%`/`起手 Y` 均 false；默认 skill（default-fallback）直接返回 prompt.js，改它即改默认链路。
- 待办登记：`checkAgentHandler.js:227-229` 用了未 import 的 fs/path（空 catch 吞，易错点检索静默失效）；liyongle skill 写死四区百分比与 boardLayout 真源冲突（非默认）；`BoardPreviewApp.vue:827` 把对象 board 当字符串渲染会显示 [object Object]；渲染层自然排版未实现。

## 汇总：夏夏（用户）原话与口径索引
- 全仓检索「夏夏 / 用户原话 / 用户明确 / 用户确认 / 用户要求 / 用户纠正 / 用户拍板 / 用户定真相」，产出 `truth/00-夏夏原话汇总.md`：10 主题 74 条，每条带出处+上下文，可信度分级（①直接引语>②署名记录>③转述口径），索引性质非第二真相。
- 本轮新增已补入：A1 meta.json 12 条原文、A2 逐条裁决结论（准5/过时或错3/渲染层归位2）、A3 夏夏最新提议「每区域只给 agent 1 个起手坐标」并确认方向可行。
- 产出 `truth/00-夏夏原话汇总.md`（74 条，分 11 类：协作方式/真相源/画布字体字号行高/板书契约/动作标记/手稿渲染/handoff截图修缮/交付产物/教学口播/代码署名/待拍板）。性质=索引不是第二真相，每条带出处与现状，可信度分级 ①直接引语 ②署名记录 ③转述口径。
- 检索口径：全仓 `夏夏|用户原话|用户明确|用户确认|用户要求|用户纠正|用户拍板|用户定真相` + 通读 CONTINUITY.md / PROJECT_STATE.md / ENGINEERING_LOG.md / truth/* / .codebuddy/memory/* / skills/board-speech-rules/meta.json。
- 关键发现：`meta.json:29/48` 仍写 board `{startCoord, content}`，与本次契约收口冲突（已过时待更新）；`meta.json:36/37` 与 `truth/territory-C-actions-and-anchors.md:25/40` 是同一批夏夏原话的两处互证。
- 6 条待夏夏拍板：板书字号 35px vs 1.2~1.5 倍；meta.json 12 条未落码；坐标硬软约束终局；handdraw-player/lite-player 缺失；checkAgentHandler fs/path；R2/R4/R5 清理。

## 用户对本轮 6 条的裁决（逐条）
1. 字号 → **由当下 agent 决定，同一 row 必须一致**（35px 退为兜底）。⚠️ 与 board 只有 content+startDelay 的契约冲突，是否加行级 fontSize 待确认，未改代码。
2. meta.json 12 条 → 用户答"是"，语义未明（确认落码？还是确认未落码？），已挡回待澄清。
3. 坐标约束 → 用户答"对"：可超出本区，不得重叠/溢出，硬约束只留给 actionSpec。已定终局。
4. handdraw-player/lite-player → 用户答"4 我删的"：主动删除不是缺陷。但 4 处引用仍在（vite 入口、AgentBDirect:1374 window.open、renderDeliverableHtml:6 模板→降级页自身是死链、public/deliverable/*.html:9 跳转），是否清引用待定。
5. checkAgentHandler fs/path → 用户问"怎么回事"，已定位：`checkAgentHandler.js:11-27` 无 fs/path import，227-229 使用 → ReferenceError 被 241-243 空 catch 吞 → 知识库易错点永不进 Check，恒用 4 条兜底。`doc/knowledge-a.compact.json` 结构正常（rows[247]，k+m）。第二层坑：匹配用 `problemText.includes(知识点名)`，知识点名过长，命中率极低。
6. R2/R4/R5 → "有用！大用！不可删除！"：改为保留能力+补实现，禁止删除处置。
- 全部裁决已写入 `truth/00-夏夏原话汇总.md` 第十一节 + MEMORY.md「风险处置口径」。

## 骨架改造：双 CDN 动态加载与多级降级兜底落地
- 遵循 CDN 五原则：主 CDN（字节跳动/静态源）+ 备 CDN（BootCDN）+ 本地兜底（public/ 或系统字体）。
- 落地模块 `src/utils/cdnLoader.js`：
  - `loadCssWithFallback` / `loadScriptWithFallback`，支持超时熔断与全局变量挂载校验，多级降级防页面挂起。
- 改造消费点：
  - `src/board-tools/boardTypography.js`：`ensureHandwritingFont` 接入 `loadCssWithFallback`，外链字体加载异常自动回退系统楷体栈，不阻塞主流程。
  - `index.html` 与 `board-preview.html`：对 `zeoseven` 字体外链注入 `onerror` 容错监听，异常时降级报警，杜绝网络阻塞渲染。
- 验证：lint 0 错，代码按需加载与容错完备。
