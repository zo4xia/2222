# 2026-09-15

## 最终收敛观点：先修画布入口，冻结 draw 生产者扩展
- 红队复核后最终采用：`RealBoardPreview.vue:41-43` 的 `resolveCanvas()` 返回 `boardRef.value`（HTMLElement 容器），不放宽两个工具内部的 SVG/HTMLElement 守卫；这是最小、最符合既有 rough 工具语义的修法。
- 执行顺序：①只改画布入口；②在 dev 3200 浏览器实测既有 `rough-line/arrow` 出图；③再判断 R2 `draw` 的真实执行。此前不做重复函数重构，不改 Agent B prompt/契约。
- `draw` 保留能力但冻结为预留：当前全仓没有生产者输出 `tool:"draw"`，向 Agent B 开放会越过 C 领地 4 工具白名单，必须另行拍板，不能借 R2 实现顺手接入。
- L2 `startCoord` 结论降级：当前仅写入侧成立，src 应用消费侧为零；不得再称「端到端已接通」。
- 本轮最终主线：**修 P0a → 真机验收既有绘制 → 再决定 R2 是否有生产者 → 其余 R4/R5/字号/排版全部冻结。**

## 红队（0 置信 code-explorer）复盘：部分推翻 P0 结论 + 坐实 draw 无生产者
- **修正 1（机制分工）**：L3「一个都不画」成立，但两条路径不同。rough-line/arrow 在 **prepare 期**抛错（`roughDrawingTool.js:264`）→ `boardToolCatalog.js:92` catch → `fallback-noop` + `console.warn`；draw 在 **execute 期**抛错（`drawIntentTool.js:125-127`）→ 发生在 `handActionScheduler.js:47`，只有 `:60 tail.catch` 保链，**promise 仍 reject** 且 `RealBoardPreview.vue:125` 丢弃返回值 → 控制台未处理 rejection。不是「都被 :92 吞」。
- **修正 2（修法改判）**：我原倾向 A（放宽 `instanceof Element`）**错**，采信红队的 C：`resolveCanvas()` 应返回 `boardRef`（`RealBoardPreview.vue:171-176` 的 `div.board`，HTMLElement，内部含 `:202` 的 svg）。已复核确证：boardRef 绑在 div 上（非 svg），改后 querySelector 能命中已有 overlay，既符合 rough 侧「canvas=HTMLElement 容器」的原设计语义，又不动守卫。**一行修复**。
- **修正 3（🚨 比 P0 更根本）**：全仓 grep `tool:'draw'` / `DRAW_INTENT_TOOL_ID` **只在 `src/board-tools/` 内部出现**（定义/校验/catalog 分发），**没有任何 prompt、skill 或 example 产出 `tool:"draw"`**（`src/agent-b-v2` 技能只开放 rough-notation / rough-line / rough-arrow）。→ **draw 是「未接线」而非「画不出」：没有生产者，我接的执行层目前是空转能力**。
- 判定：R2 真实状态 = 「执行已写、数据源为零、渲染层还被守卫挡着」三重空转。夏夏裁定 R2「有用、大用、不可删除」的前提（有人用）当前不成立。
- **结论 3 补偿**：L2 `startCoord` 写回**仅写侧成立、消费侧为零** —— src/ 内零消费者（`RealBoardPreview.vue:108-115` 只按 region 排版不读它），唯一读它的是根目录两个独立 demo HTML，而全仓对这两个文件 **0 引用**。故 DIGEST/记忆里「L2 写回已接通」名不副实，需降级表述。
- 全仓 **0 个 test 文件**（`*.test/spec` 0 命中）→ 任何「复用后行为不变」的说法都无运行时证据，只能靠真机验。

## 🚨 P0（三方取证 + 主 agent 交叉验证）：L3 rough 绘制层整体静默失效
- 证据链（两处独立）：
  1. `RealBoardPreview.vue:41-43` `resolveCanvas()` 返回 `roughSvgRef.value`，而 `:202-209` 该 ref 是 `<svg data-board-overlay="rough-drawings">` → **SVGElement，不是 HTMLElement**（JS 规范：SVGElement 继承 Element，不继承 HTMLElement）。
  2. `roughDrawingTool.js:143` 与 `drawIntentTool.js:38` 守卫均为 `canvas instanceof HTMLElement` → 恒 false → **抛错**。
- 后果：抛错被 `boardToolCatalog.js:92-100` 的 catch 吞掉并降级 `fallback-noop` → **既不画也不报错**。即：既有的 `rough-line`/`arrow` 与本次新接的 `draw` 在真实页面里**全部静默不画**。R2「已接通」在运行时为**假**。
- 影响面：L3 所有手绘动作；与 DIGEST 第 7 节「R1 四区锚点缺浏览器级人工验收」同源——该层从未真机验证过。
- 修法候选（**未动代码，待夏夏放行**）：
  A（最小）：守卫放宽为 `canvas instanceof Element`，且「若传入节点本身即带 `data-board-overlay="rough-drawings"` 则直接返回它」——同时修两处（roughDrawingTool + drawIntentTool）。
  B：改 `resolveCanvas` 返回外层 `div.board-layer-l3`，让 querySelector 找到 svg。
- 结论：优先 A（改一处语义，两个工具同时复活）。

## 落地：R2 draw 意图工具接通真实执行（消除空壳死重，保留能力不删除）
- 依据：夏夏对 R2 裁定「有用！大用！不可删除！」→ 处置方式=接通真实执行，不是删除工具。
- 改动（仅 2 个文件）：
  1. `src/board-tools/drawIntentTool.js`：保留 `getDrawIntentAgentTool` / `validateDrawIntentAction`，新增 `parseDrawIntent`（分号分隔 key:value 意图解析）、`parseVertices`、`parseCoordPair`；实现 `prepareDrawIntentAction(action, { resolveCanvas })` —— 复用 `rough.js` + L3 `[data-board-overlay="rough-drawings"]` 图层，绘制多边形(≥3 顶点)/线段(2 顶点)/梯形·三角形·矩形·椭圆兜底 + 顶点文字标签 + `stroke-dashoffset` 渐进动画；时长优先读 intent 的 `时长:Xs`，兜底 1200ms。
  2. `src/board-tools/boardToolCatalog.js`：import `prepareDrawIntentAction`，把 `createBoardToolRuntime` 里对 `DRAW_INTENT_TOOL_ID` 返回的空壳 `{ execute: () => {}, remove: () => {} }` 换成真实 plan。
- 验证：lint 两文件 0 错；node 实测 `getAgentBoardToolCatalog()` = 4 类工具、`validateBoardToolAction(draw)` = ok、`runtime.enqueue()` 入队成功。**未做浏览器真画布视觉验证**（无 DOM 环境，execute 走 `resolveCanvas` null 分支）。
- ⚠️ 记忆订正：本条**曾误写入 `2026-09-14.md`**，且插在「盘点：public/ 画布素材与引用对照」标题与正文之间打断了原文；已撤回，09-14 原文已复原，正文迁到本文件。教训：追加日志前先确认当日文件是否存在（今日文件已存在，不应写昨日）。

## ayuan-aide 首次上班（DIGEST 复核 + 珍珠卡，只读校验后仅写两个文件）
- 逐条复核 DIGEST 第 0 节 8 条纠错：**8/8 仍成立，无一被推翻**。第 1 条二次取证成立：数据侧 `startCoord`(DEMO:155)，代码侧 `:583 parseCoord(b.coord)` / `:611 parseCoord(task.coord)`，恒回落 `{x:8,y:44}`。其余取证：`stepHandoff.js:16/24-25`、`AgentBDirect.vue:1223`、`deliverableStoreHandler.js:150-160`、`contract.js:131-137`、`timing.js:79-84`、`AgentBDirect.vue:598-621`、`checkAgentHandler.js:11/13/20/42 缺 fs/path 而 :227-229 在用`、lecture `:543/:305/:207-211`、MEMORY.md `:15 vs :77`。
- 全 src grep `playAll|连播|播放全部|autoPlay` 仅 `RealBoardPreview.vue` 一处 prop → **无整篇连播编排器**实证成立。
- 第 8 节改写：CONTINUITY.md **已恢复**，实测 22101 字节（`git checkout --`），关闭待拍板第 5 项；新增待办「video-dev-src 入库」与「删 context-digest-writer.md」。
- 实测补数：`D:\video-dev-restored\src.zip` 5985014 字节；`%TEMP%\video-dev-src` 228 文件（顶层 `scripts/`/`src/`）；skill、ayuan-aide.md、identity.md 均存在。
- DIGEST 改后 **76 行**（远低于 250 上限）。珍珠卡落 `E:\zeta-family\agents\historian\pearls\sessions\2026-09-15_讲题板书-三层契约与音频驱动.md`。
- ⚠️ 遗留冲突（未改代码）：板书字号裁决 1.2~1.5 倍 vs `BOARD_FONT_SIZE=35` 写死；`MEMORY.md:15` 端口表述与 `:77` 矛盾待订正。

## 全局复盘（对话与历史 session 梳理，只读，未改运行代码）
- 材料源：`ENGINEERING_LOG.md`（08-16→09-14）、`DECISIONS.md`、`PROJECT_STATE.md`、`KNOWN_ISSUES.md`、`truth/*`、`.codebuddy/memory/*`、git（仅 9 个 commit，历史很浅）。
- ⚠️ **重大发现：`CONTINUITY.md` 工作区文件为 0 字节**（此前大量口径引用它如 CONTINUITY.md:240/321/352）。`git show HEAD:CONTINUITY.md` 仍有 295 行 / 11866 字符可恢复（HEAD 提交 fa5a0bc，文件在 f03c38d「清理」被改）。**是否恢复待夏夏确认**，未擅自操作。
- 复盘主线结论：
  1. 项目长期病症是「多套真相并存」（字号/行高/坐标/四区/字体/canvasParams），09-14 已用 `stepHandoff.js` 常量收敛成功，该模式应推广到坐标与字体。
  2. 演进方向稳定且不可逆：**把模型算不了的东西逐步剥离给渲染层**（duration→startDelay→起手坐标→行距抖动），最新共识是「每区域 1 个起手坐标 + 代码流式排版」。
  3. 反复踩坑：验证端口 3000 属另一个项目（应用 3200/3001/5199）、demo.html 测试页被误当真相、`vite build` 缺 lite-player.html、缺失 handdraw-player.html 致按钮 404、空 catch 吞异常。
  4. 待拍板未变：板书字号终值（35px vs 1.2~1.5 倍）、meta.json 12 条是否落码、渲染层自然排版未实现、R2/R4/R5、两个缺失播放器模板。

## 外部交稿播放器契约实测（夏夏提供 5 个文件，只读）
- 真正消费本仓 JSON 的 3 个：`lite-player.html`（接 `public/board-result/current.json`，UI 显示「🎵 MP3 时长 + ✍️ +n 秒板书起手延时」，含「重新静默预生成全部 MP3」）、`白板播放器-平行四边形DEMO.html`（要 `board.startCoord` "[6%, 12%]"、`estimatedDurationMs`、`speechCharacters`、`plan[{type,startOffsetMs,durationMs}]`、`totalDurationMs`、`speechRateCpm:160`；头部注明「时钟：audio.currentTime > estimatedDurationMs > 字符数估算(告警)」）、`lecture-动作不行.html`（`parseCoord(rows[index].board?.startCoord)`，内嵌我方 deliverable apiSpecVersion 2.0.0）。
- 非契约类：`ref_whiteboard.html`（纯画板笔触参考）、`handwrite-compare.html`（字体对比，无契约字段）。
- **结论（解除"要不要 mp3/时长"之争）**：对外交付 JSON 必须带 `startCoord` + `audioUrl` + `estimatedDurationMs`，但**这些都由程序在流水线后段补，模型仍不产**。`deliverableStoreHandler.js:150-160` 已在写 `estimatedDurationMs` 与 `exclusiveExecutionPlan`（程序算），`timing.js`/`speechTiming.js` 已有算法。
- **真实断点（3 个）**：① `board.startCoord` 自 09-14 剥离后新产物已无 → 三个播放器全部依赖它，必须补"排版写回"环节；② `audioUrl` 被 `contract.js` 归一化丢弃（只保留 stage/speech/board/actionSpec 4 字段），需透传；③ 外部用 `plan[]`/`totalDurationMs` 命名，内部是 `exclusiveExecutionPlan`，需对齐或做映射。
- 三层契约定案：L1 模型输出（4 字段干净）→ L2 流水线加工（补 startCoord/audioUrl/duration/plan）→ L3 对外交付。

## 新架构方向（夏夏拍板）：音频驱动 row-by-row 播放（2026-09-15）
- 夏夏原话：「原先没有音频 api，现在有了，所以我们以音频自然播放 row by row 多好」→ 采纳为播放层终局方向。
- **代码已铺好一半（实证）**：`timing.js:79-84` 时长真相源已是 `row.audioDurationMs`（有实测就优先，否则才 160cpm 字符估算兜底）；`AgentBDirect.vue:598-621` `readAudioDuration()` 用 `loadedmetadata` 读真实 `audio.duration` 回填 `record.audioDurationMs` 并重算时间线；`playAudioUrl()` 注释明确「只由原生 ended 事件结束，不使用预估时长或定时器切换」。
- **缺口**：全仓无整篇连播编排器（grep `playAll|连播|播放全部|autoPlay` 仅 `RealBoardPreview.vue` 一个 autoPlay prop），目前只有单行试听。
- 设计：每行 `audio.play()` → 到 `startDelay` 触发板书 → 板书完按 `order` 做动作 → `ended` 事件驱动 i++ 下一行；全局时钟彻底废弃（不再需要 rowGapMs / 总时长预估 / 模型估时长）。
- 三个必须处理的边界：①无音频降级（保留 160cpm 估算 + 定时器）；②板书长于音频（书写速度自适应音频剩余时长，而非死守 1 秒 2 字）；③断点续播（天然支持，顺带解决 R5）。

## 小史官体系接入（夏夏家制度）
- 已安装 skill：`C:\Users\zo4xi\.codebuddy\skills\xiaxia-historian\`（SKILL.md + agents/openai.yaml）；身份根 `E:\zeta-family\agents\historian\identity.md` 存在。左右脑定义：阿圆=左脑向前施工，小史官=右脑守历史与方向（互补非抢活）。
- **`CONTINUITY.md` 已从 git 恢复**（`git checkout -- CONTINUITY.md`，现 22101 字节，此前为 0 字节）。
- **已建专属小跟班 agent**：`C:\Users\zo4xi\.codebuddy\agents\ayuan-aide.md` —— 以小史官为母本、按本机校准（用户 skills 改 `C:\Users\zo4xi\.codebuddy\skills\`；本仓端口 3001/3200/5199；开发期禁 build）。唯一可写 = 项目 `CONTEXT-DIGEST.md`（唯一 owner）+ 当日 memory（追加）+ `E:\zeta-family\...\pearls\sessions\*`（追加）；禁止碰 src/ server/ 等运行代码；含偏航报警与 A/B 轨交卷格式。
- ⚠️ **已解决**：`context-digest-writer.md` 已删除（2026-09-15），owner 冲突消除。唯一写手 = `ayuan-aide`。
- 小史官的 `Invoke-HistorianCheckpoint.ps1` 会推送远端 `lllyyysss/my-room`，**未执行、不自动执行**，需夏夏明确授权。
- **skill 已装齐（只复制，源 `E:\zeta-family` 与本机 `C:\Users\zo4xi\.codex` 均未改动）**：`xiaxia`、`xiaxia-anchor-marking`、`xiaxia-context-compression`、`xiaxia-continuity`、`xiaxia-draw`、`xiaxia-historian`、`xiaxia-persona`、`xiaxia-truth-guard`、`headroom`（源名 headroom-skill-main）、`lean-ctx`。副本内旧 `.codex` 路径**暂未替换**（夏夏说不要动原来 codex 的，待确认）。
- **小跟班首次交卷**：DIGEST 压缩到 76 行；珍珠卡落 `E:\zeta-family\agents\historian\pearls\sessions\2026-09-15_讲题板书-三层契约与音频驱动.md`；第 0 节 8 条纠错 **8/8 仍成立**（DEMO:583/611 读 `task.coord` 非 `startCoord` 二次确证）。
- **外部模块已入库**（防 %TEMP% 清理）：`truth/_reference/video-dev-modules/` 9 个文件（renderBoardTextStickerImage / boardStickerGeometry / mathBoardText / getBoardRevealProgress / normalizeBoardRevealWindow / boardRevealConfig / useVoiceTrackAudio / voiceTiming / scriptRow）。
- **MEMORY.md:15 已订正**：dev 端口口径改为「本仓 3001/3200/5199，3000 是另一个项目」。
- **✅ 排版器已落地（L2 断点补齐）**：按 reuse-first-guard 复用检查后**未新建模块**，收口进 `src/utils/boardLayout.js` 的 `layoutBoardRows()`（P4 公共模块统一收口）。复用项：四区坐标 `BOARD_LAYOUT`、边界 `BOARD_BOTTOM_LIMIT_PCT`、字号真源 `BOARD_FONT_SIZE`；未复用 canvasCoords（百分比直接算）。规则：区域即起点 + 纵向按自然行高累加（无人为行间距）、游标单调递增天然防重叠、触底 clamp 到 95%。实测 5 行数据：题目 13.2% / 分析 41% / 解答 14%→22.57% / 总结 69.5%，全部 ≤95%。
- **L2 写回已接通**：`server/deliverableStoreHandler.js` `writeDeliverableFile` 中调用 `layoutBoardRows` 并写回 `board.startCoord`（已有值不覆盖）。node 实测 handler 加载无环、lint 0 错。
- ⚠️ **遗留（时间所限未做）**：①`board.place` 意图层（zone/near/avoid/flow）中 `near/avoid/flow` 属自造概念，违反 reuse-first-guard Guardrail 2，**已主动拦截不做**，待夏夏给可溯源命名；②前端精确度量（A 方案）未做，当前为 server 端估算（中文全角 1 字宽、ASCII 0.55 字宽，误差 <5%）。
- **自动化已建**：`小跟班定期纠偏`（FREQ=HOURLY;INTERVAL=1，平台不支持 20 分钟粒度，已按最接近的每小时），每小时召回 `board-lecture-aide` 做轻量巡检 + 偏航报警。
- **「每家都有自己的小跟班」已落地**：新建本仓专属小跟班 `.codebuddy/agents/board-lecture-aide.md`（项目级）。分工：用户级 `ayuan-aide` = 通用左右脑影子（跨项目规矩）；项目级 `board-lecture-aide` = 带本仓三层契约/真源/领地/专属坑的分身。冲突优先级：本仓运行时代码 > 项目小跟班 > 用户级小跟班 > 记忆。

## 渲染层排版方案（夏夏质疑后收敛，待最终拍板）
- 夏夏质疑：「已经给了四区坐标，是否真的需要定起手？溢出是允许的，只要不出画布」。
- **结论：取消「每区 1 个起手坐标」这个冗余概念** —— 区域 bounds 自带起点，起手不该由人/模型/配置指定，应由代码从地皮推导。原提议简化为「**区域即起点 + 代码纵向累加**」。
- 待实现的是**排版器**（纯函数，不是坐标配置）：`layoutBoardRows(rows, boardPlan, canvasSize) → 每行 startCoord`，在 L2 阶段写回产物（外部播放器仍要 startCoord，只是"算的"不是"定的"）。
- 规则草案：①起点 = 区域 bounds 左上 + 内边距；②**纵向推进量 = 自然文本换行行高（夏夏取消人为行间距，不额外加行距；微抖动归渲染层，不进 layout）**；③横向按区域 w 自动换行（不是溢出）；④纵超出区域 → 允许溢出，但不得与已排板书 bbox 相交（相交则右移/下移）、不得出画布（触底则缩小字号或换列）；⑤题图避让（有 screenshotUrl 时起点下移到图下方，MVP 可后置）。
- ⚠️ **实现位置待定（关键）**：「自然换行」只有真渲染（canvas measureText）才精确，而 server 端无 canvas。三选一：**A 前端排版**（导出前用 canvas 度量写回 startCoord，最准，改 `AgentBDirect.vue`；可用 `renderBoardTextStickerImage` 返回的真实 height）；**B server 估算**（中文方块字好估，MVP 快，需把估算系数收进 `stepHandoff.js` 真源）；**C 双层**（server 估算兜底 + 前端精确重排）。倾向 A 精确 / B 先跑通。

## 排版方案再演进：从「写死规则」转向「agent 给意图 + 代码保物理」（2026-09-15 夏夏定调）
- 夏夏定调原话：「因为没办法照顾所有情况那么不如相信小 agent 们的能力」。
- **关键辨析（我方补充，夏夏未反驳前按此执行）**：相信 agent ≠ 让 agent 算坐标。分工应为 **agent 给「布局意图」（决策权），代码做「物理落点」（数值权）+ 几何兜底**。模型仍不碰任何像素/百分比数值。
- **契约草案**：`board` 增加**可选** `place` 字段（松门槛，不填则用区域默认）：
  `{ zone: 'solution', near: '上一步的算式', avoid: '题目配图', flow: 'down'|'right' }`
  语义：zone=想落哪个区；near=挨着哪块已有内容（语义锚）；avoid=避开什么；flow=写不下往哪延展。
- **代码只需 3 条兜底**（取代原先 5 条穷举）：①意图解析失败/未填 → 区域默认起点；②不得与已排板书 bbox 重叠；③不得出画布。不再穷举"行距/抖动/避让"等覆盖不全的规则。
- **与既有定论的关系**：board 仍只有 `content` + `startDelay` 两个**必需**字段，`place` 是可选软引导，符合松门槛（格式硬约束、内容软引导）。

## ⚠️ 已驳回：小 agent 提议「让模型输出 mp3 + duration」（2026-09-15）
- 来源：某小 agent 写的提示词优化建议（要求模型按 speech 字数**估算** duration 秒数并填 `mp3`）。**已驳回，不得采纳。**
- **五处硬伤（均有证据）**：①让模型估时长 → 违反 DECISIONS 2026-08-21「时间不预计算」与 `truth/00-TRUTH-BASE.md` 领地 A 铁律「B 不计算毫秒」；②与同份 prompt 5.2「禁止 durationMs」自相矛盾；③「五个核心槽位」却列 6 个；④mp3 由 `fishAudioHandler` 运行时生成，模型只能留空；⑤**字段命名与外部播放器不对齐**（外部要 `estimatedDurationMs`/毫秒/`audioUrl`，它给 `duration`/秒/`mp3`）。
- **正确做法**：模型**一个都不填**；内部用 `audioUrl` + `audioDurationMs`（实测毫秒，`timing.js:79-84` 优先用实测、无音频才 160cpm 兜底）；导出时 `deliverableStoreHandler.js:156` 已映射为外部认的 `estimatedDurationMs`。
- **教训（写入小跟班偏航条件）**：小 agent 适合提需求/写草稿，但**契约类改动必须回源核对代码与决策记录**，否则会用"看起来合理"的方式推翻已定铁律。

## 外部参考源：D:\video-dev-restored\src.zip（夏夏提供，228 文件，React+TSX 教学视频工程，已解压到 %TEMP%\video-dev-src 待移植）
- **可原样移植的纯函数（无框架依赖，文件头标 `@cleanroom-module` + `@boundary`）**：
  1. `src/modules/boardSticker/renderBoardTextStickerImage.ts`（267 行）— 达芬奇手稿感三路**确定性**抖动（字号 ±4.5% / 基线 ±8% / 字距 ±8%，seed=行号+文本+字号，LCG 因此不闪烁）、自动换行(maxTextWidth)、行高 1.38 + jitterPadding、LRU 缓存；**返回值含真实 width/height → 可作"每区 1 个起手坐标 + 代码流式排版"的高度累加器**。
  2. `src/modules/boardReveal/getBoardRevealProgress.ts` — smoothstep 缓动 + drawSpeed 倍率(0.1~4)，揭示进度。
  3. `src/modules/boardReveal/normalizeBoardRevealWindow.ts` — 揭示窗口∩显示窗口求交 + 边界钳制（"写不完"的边界已处理）。
  4. `src/modules/boardSticker/boardStickerGeometry.ts` — xPercent/yPercent/widthPercent/fontSize/drawSpeed 归一（= 我们的 startCoord 语义）。
- **契约参考（不可直接套用，需映射）**：`src/protocols/voiceTiming.ts` 的 `VoiceTimingSlice{rowId,audioUrl,startMs,endMs,durationMs}` 正是"音频驱动 row-by-row"的数据形状；`src/protocols/scriptRow.ts` 类似我们 rows。
- **逻辑可移植但需重写**：`src/modules/audioPlayback/useVoiceTrackAudio.ts`（React Hook）— 播放时钟锚点 `playbackClockRef{clipStartMs,clipEndMs,offsetMs,startedAtMs}` 用 `performance.now()` 对齐音频而非估算；配套 `voiceAudioSeek.ts`（seek 续播）、`voicePlaybackStart.ts`、`activeVoiceAudioElement.ts`。
- **管线参考**：`src/modules/timeline-factory/*`（splitScriptIntoTtsSentenceUnits → createTtsBatchJobs → orderTtsSentenceResults → createBoardEventsFromTtsUnits → mapBoardEventsToTimelineClips → mergeBoardClipsIntoTimelineClips）= 我们要补的 L2「程序补时长/plan」层。
- **验收护栏**：`scripts/check-*.mjs` 27 个，重点 `check-board-boundaries.mjs`(49KB)、`check-script-agent-rows-contract.mjs`(24KB)、`check-board-handwriting-support.mjs`、`check-timeline-window-contract.mjs`。
- **不兼容点**：对方 React+Konva/tldraw，我们 Vue3+原生 Canvas+rough.js → 只抄纯函数模块，组件不搬；契约走映射层，我们自己的 rows 四字段不动。

## DEMO 播放器（白板播放器-平行四边形DEMO.html，已复制进工作区根）诊断：「有点意思但太死太硬」
- 夏夏评价原话：「这个有一点那个意思，但是太死了太硬」。
- **做对要保留的 6 点**（行号据工作区副本）：确定性抖动 mulberry32+seed 幂等可 seek(364-367/491-503)；「一行一张贴纸图 + 斜梯形遮罩按 progress 揭示」(466-517/639-678)；FEATHER_PX=2 切口羽化软边(608/666-676)；rline/rrect/rellipse 手绘几何且椭圆「略过一点留没画满手感」(399-431)；LaTeX→Unicode 规范化 + 结构数学告警清单 + HTML 实体/转义损伤修复(523-560)；stage 标签走内容锚不是区域角(575-603)；DPR 2~3 + LRU 120。
- **诊断"死/硬"5 处**：①时间线靠 `dur=estimatedDurationMs` 或 160cpm 估算 + `BASE_CHAR_MS=400` 匀速(381-385/607)，无音频驱动（有 audioEl 但只播放不驱动）；②揭示进度 `pr` 是**线性**映射到行宽(644)，只有斜边斜率随机 → 匀速扫描感，无起收笔缓动、无标点停顿；③位置写死 `parseCoord(task.coord)` + `const size=38`(611-613)，无行级微旋/行首 x 抖动/字号按行随机；④行间只有 HAND_LIFT_MS=600 静止，缺"写完停顿看一眼"的呼吸；⑤38px/行高1.7 与我们真源(35px/自然换行)冲突。
- **改造优先级**：A 接音频时钟（p 改由 audio.currentTime 驱动，无音频降级估算）→ B 书写缓动（逐字 + 起收笔 ease + 标点加权停顿）→ C 行级姿态（±0.3~0.8° 微旋 + 行首 x ±0.5~1.2% + 字号按行 ±3%）→ D 配速自适应（写不完时按音频剩余时长压缩每字时长）→ E 参数接 stepHandoff 真源。

## 三份交稿「各有千秋」横向盘点（夏夏原话）
- **`handwrite-compare潦草感优秀.html`（DOM/CSS 方案，最值钱）**：四档参数 B=行倾斜±1.5°/行距±10%/字±1px位移+±1°旋转；**C=「王虹范式」SVG `feTurbulence` 位移滤镜（baseFrequency .022, numOctaves 3）让笔画边缘渗透抖动 —— 关键论断「手写感来自笔触，不是把字扭歪」**；D=达芬奇 ±3°/行距±25%/字±2px/旋转±2.5°。字体走本地 `public/fonts/pingfang-qiaomu.ttf` ✅。缺点：用 `Math.random()` 不可幂等、无法 seek。
- **`lecture-动作不行，画布尺寸歪，写完板书没了。.html`（DOM 逐字揭示方案）**：✅ 逐字 `.char.revealed{opacity:1}` + transition .06s（最细揭示粒度）、✅ `.frac` inline-flex 真分数排版、✅ 时长自适应 scale(369-376)。❌ 三个 bug 已定位行号：**①「写完板书没了」= `543: board.querySelectorAll('.board-content').forEach(el=>el.remove())` 每次切 row 全清，板书不累积；②「画布尺寸歪」= `305: board.style.transform=scale()` 未设 `transform-origin: top left`，transform 不改布局盒 → 占位仍 1726×980 导致留白/溢出；③ 动作层 `542: svgOverlay.innerHTML=''` 与板书一起被清 + ACTION_SEC 固定 + 坐标未随 scale 换算**。另注：`:207` 仍外链 `i.ibb.co`（违反 9-14「禁境外资源」）。
- **合并目标架构**：DEMO 骨架（canvas+贴纸图+seed 确定性+遮罩揭示+手绘几何+LRU）+ compare 的 7 路抖动参数与 feTurbulence 笔触层（**随机数改 seed 派生以保幂等**）+ lecture 的逐字揭示与 frac 真分数 + 修掉 lecture 那两个 bug + 我们自定的音频驱动。

## 当前遗留扫描（2026-09-15）
- 双路只读审计交叉确认：最高优先级是 `npm run build` 被不存在的 `lite-player.html` 入口阻断；交付播放器 `handdraw-player.html` 多处仍是确定死链；`RealBoardPreview` 将 SVG 传给要求 HTMLElement 的 rough/draw 工具，且区域边界还存在百分比与设计像素单位不一致，绘图会降级为空动作。
- 业务一致性遗留：knowledge refine 接口仍直接覆盖 handoff；UI canvasParams 与 handoff canvasParams 并存；`question/topic` 区域命名及 handoff 白名单仍有重复语义；导出与渲染可能读取不同参数源；relatedKnowledge 结构不统一。
- 播放与验收缺口：当前未证实 row-by-row 自动连播；exactText 跨 DOM 文本节点会失败；无真实 HTTP 集成测试；四区 exactText 浏览器验收、生产服务器只读预检、L4 画笔和完整板书预排版/遮罩揭示仍未闭环。
- `KNOWN_ISSUES.md` 中 P0-2 与 P2-5 与现行代码存在冲突，暂定为“待定向验证”，不能直接作为现状结论。本轮未修改运行代码或项目状态文档。

## 运行核验（2026-09-15）
- 本仓 Vite 实例 `3200`、`5199` 均由当前工作区启动，首页与 `board-preview.html` 返回 HTTP 200；`/src/main.js`、`/api/deliverable/list`、`/api/handoff` 也返回 200。
- `3000` 是另一项目，不能作为本仓依据；`3100` 进程存在但根页面请求失败，不能判定生产服务可用。
- 未执行 build：`dist/` 与 `lite-player.html` 均不存在，且历史日志记录 `vite.config.js` 配置引用未存在入口、`src/check-agent/prompt.js` 曾有解析错误；开发期遵循只跑 Vite dev。
